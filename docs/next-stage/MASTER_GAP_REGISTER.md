# AAFC TMS — Master Gap Register

**Version:** 2026-08-15  
**Branch:** `next-stage/v1-operational` | Deployed commit: `756e65e` (production, 2026-08-12, Alembic head v51)  
**Backend tests:** 1709 passed, 6 skipped (2026-08-15, post CLASS-21 — confirmed at HEAD `b0e2273`). Regression test added 2026-08-15: `test_annual_program_empty_year_no_parade_nights` (commit `fe5a757`).  
**E2E tests:** `frontend/e2e/planning-workspace-filters.spec.ts` added 2026-08-15 — CLASS-19/21/22/23 filter feature coverage (commit `fe5a757`).  
**Sources merged:**
- 2026-08-12 program audit (prior register: 18 DONE / 14 PARTIAL / 57 NOT DONE / 7 HUMAN GATE)
- Training Class architecture analysis (`parallel-class-impact-analysis.md`)
- Defence Writing Manual requirements (`defence-writing-ui-standard.md`, 46-row `interface-language-inventory.csv`)
- Original 25-gap matrix (`01_gap_matrix.md`)
- UX gap register (`ux-gap-register.md`)
- Dashboard metric dictionary, workflow map, capability manifest
- Security hardening review (`15_security_hardening_review.md`)

This is the single authoritative register. The prior `01_gap_matrix.md` and `ux-gap-register.md`
are superseded by this document for status tracking; those files remain as reference and evidence.
The "35% complete" figure from the prior register is not reproduced — that figure was computed from
only the original 25-gap matrix and predates the Training Class backend work, the Defence Writing
sweep, the UX/product gaps, and the security hardening additions merged here.

---

## Status Totals (recomputed 2026-08-16 — post MAINT-03)

| Status | Count |
|---|---|
| CLOSED | 38 |
| STAGING VERIFIED | 9 |
| FIXED LOCALLY | 122 |
| IMPLEMENTING | 1 |
| NOT STARTED | 2 |
| HUMAN GATE | 20 |
| ACCEPTED RISK | 2 |
| MANUAL APPROVAL REQUIRED | 1 |
| **Total** | **195** |

**Completion rate** (CLOSED + STAGING VERIFIED + FIXED LOCALLY) **= 169 / 195 = 87%**

> 12 new items registered 2026-08-15 (Final Engineering Program): MAINT-02, AUTO-01, DATA-CONF-01, FAC-DUP-01, CLASS-MATRIX-01, CLASS-FORECAST-01, BULK-01, FAC-SUG-01, PN-WIZ-01, DND-01, DENS-01, HOL-01. All initially NOT STARTED. Completion rate will recover as items are closed.

> Counts are from rows matching `^\| [A-Z]+-[0-9]` in this file. Excludes cross-reference summary rows (numbered `1–25`) and GAP-prefixed decision rows (GAP-03, GAP-05, GAP-20, GAP-22, GAP-23) which duplicate existing HG-/SEC- items. Totals increased from 138 → 152 across sessions as new gaps were identified and added to the register; the completion rate decreased from 80% → 76% because new items were added faster than they were closed.

**2026-08-13 audit:** 8 items promoted from NOT STARTED → CLOSED/FIXED LOCALLY after code inspection (DEF-02, DEF-12, HELP-02, HELP-03, HELP-06, MBACK-03, WORK-06) plus E2E CI workflow created (VIS-02 → FIXED LOCALLY). 1 new item added (e2e-tests.yml workflow). DASH-08 promoted IMPLEMENTING → FIXED LOCALLY after confirmed code inspection of all command-dashboard drill-down paths. SEC-09, SEC-10, SEC-13, SEC-14 promoted IMPLEMENTING → STAGING VERIFIED after live HTTP checks against staging and production. SEC-05 (login spike alerting) and SEC-06 (5xx spike alerting) implemented in `security.py`/`main.py` with tests. DOC-14 (CEA operator guide) and DOC-15 (role matrix §V17 Training Class) written. WORK-08 (session move in PW) implemented: "Move to another night" collapsible section in `PlanningRightDrawer.tsx`. Cross-reference rows 1, 13, 17, 18, 19 corrected. MBACK-05 frontend wiring complete. DASH-10 (Adoption Analytics) implemented. HARD-01 (stale data on tab focus) implemented. HARD-02 (blocking alert() conversions) extended sweep — all 40+ non-system-console `alert()` calls converted to `showToast()`. WORK-11 (copy parade night sessions) implemented. WORK-12 (bulk cancel all sessions) implemented. WORK-13 (filter persistence) implemented. WRITE-12 (error/empty-state content) implemented. HARD-08 extended: Planning Workspace `window.confirm()` elimination complete — `ConfirmProvider`/`useConfirm()` context wired through `App.tsx`; all `window.confirm()` calls in `Facilitators.tsx`, `Accounts.tsx`, `PlanningRightDrawer.tsx`, `PlanningBottomDrawer.tsx` converted (8 call sites). HARD-09 extended: `promptText()` modal coverage — 4 E2E tests added. Pre-existing TypeScript error in `permissions.test.tsx` fixed (ProxySession fixture missing 2 fields). TypeScript: 0 errors. VIS-09 extended: `nav()` now manages `aria-hidden` on inactive pages (resolves heading-structure and landmark-region AT complaints); 1 unlabelled timing-block `<select>` given `aria-label="Block type"`. VIS-11 (new): Phase 7 readability pass — 12 CSS property changes in connected-frontend: body font 13→14px, form labels 10→12px, buttons enlarged to minimum 38px target height, micro-labels 9–10px→10.5–11.5px, form-group spacing 13→18px. Cross-reference rows 10 and 16 corrected from stale IMPLEMENTING to PARTIAL/FIXED LOCALLY. Backend: 1618 passed, 5 skipped (confirmed post-change).

**2026-08-13 session 2 (this session):** Status totals recomputed from code inspection — NOT STARTED corrected from stale 20 to actual 6 (all Level B/C or post-release human actions); total items increased from 137 to 147 due to additions across CLASS/HARD/WORK/WRITE/SEC/DOC areas since original count. Completion rate updated to 78%. CLASS-17 added: Training Class display and audience editing wired into `showPNDetail` (Parade Night detail modal) — the only edit path that previously lacked class support. `_populatePnDetailClasses()` function added; `savePNDetail()` updated to save audience per session with conflict detection and override prompt, matching `saveSessEdit()`'s existing pattern. Backend: 1618 passed, 5 skipped (confirmed post-class-17-change). **Session 2 additions:** (1) Compact session card (`buildPNCard`) now shows assigned Training Class names in the `sess-info` line — zero extra API calls since `trainingClasses` is already in the parade night list response; displays `· Class 1, Class 2` after the room name. (2) `_GH_DEFAULT` (Getting Help default text) expanded: Training Stage and Training Class definitions now explain why the distinction matters, give a concrete example of Senior 1/Senior 2 with independent progress, explain combined sessions (one session can serve multiple classes), and clarify how Mission Backlog uses class-level data. "Set up Training Classes" common task added. HOW TO GET STARTED step 4 made more descriptive. TypeScript: 0 errors.

**2026-08-13 session 3 (this session):** Wing Onboarding Runbook (`10_wing_onboarding_runbook.md`) updated (commit `edaee16`): §2.0 added for `POST /api/system/provision-wing` one-call endpoint, §5 production activation updated, §7.4 and §8 updated. Gap register DOC-12 updated with runbook evidence. Backend test count: 1629 (was 1628) — new `test_provision_wing_forbidden_national_admin` test added. E2E test added in `weekly-program-classes.spec.ts` for DEF-06 dynamic wing_name header. Security/XSS sweep: found and fixed 5 unescaped user-supplied fields in facilitator table (`rank`, `first`, `last`, `type`) and room type badge (stored XSS potential, commit `05887e0`); 3 unescaped `pn.term` values in buildPNCard/move-session/WP dropdowns. HARD-02 extended: `doDisableAccount`, `doReactivateAccount`, `doUnlockAccount` error-path `alert()` calls (multi-role, missed in prior sweep) converted to `showToast()`; `gate()` fallback also converted. Total: 122/157 = 78% complete (no status count changes — these were previously unregistered items now fixed at commit time rather than being tracked as new gaps).

**2026-08-13 session 4 (this session):** XSS sweep completed — two rounds of stored XSS patching applied. Round 1 (commit `16a055d`): `s.exp`, `s.facName`, `s.room` (session fields across `buildPNCard`, `showDrill`, `renderWP`, `showCurrDetail`, `showFacProfile`), `e.instr` (curriculum detail modal), `e.lh` (Learning Hub URLs — `safeUrl()` helper added to block `javascript:` URI injection). Round 2 (commit `0ef96da`): `elBadge()` and `phBadge()` helper functions wrapped (cascading fix to 12+ innerHTML call sites each — calendar chips, curriculum list, session cards, modals, annual program); `facDisplay(f)` in facilitator profile modal; `f.type` in facilitator profile modal; subject-area tag fallback `EL_L[a]||a` in facilitator profile modal; `e.term` in curriculum list view and detail modal; unit type badge in organisations table; `a.role`, `a.action`, `a.object_type` in audit log table. Pattern documented: `${DICT[val]||val}` inside innerHTML must be `${esc(DICT[val]||val)}`. React Planning Workspace confirmed clean (no `dangerouslySetInnerHTML`). All remaining innerHTML patterns verified safe (hardcoded strings, server-generated ISO dates, numbers).

**2026-08-16 session (post-program — MAINT-03):** MAINT-03 FIXED LOCALLY — system_admin lockout edge case during maintenance LOCKED. Root cause: `maintenance_gate` middleware used JWT extraction to identify SA bypass; login requests carry no JWT, so the bypass never fires during login — SA gets locked out of their own system. Fix: `/api/auth/login` added to `_MAINTENANCE_ALWAYS_EXEMPT` frozenset in `main.py` (handler now owns the login gate); `_check_maintenance_login_gate(role, db)` added to `backend/app/routers/auth.py` and called in `login()` AFTER access code verified and role known; SA always passes; non-SA in LOCKED phase gets HTTP 503 `maintenance_mode`. Inlines `_compute_phase()` logic in `auth.py` to avoid circular import (main.py imports routers). 3 new regression tests in `test_system_admin.py`: block_logins blocks sqn_admin; block_logins allows SA; PENDING phase allows all. 1 pre-existing test in `test_maintenance_enforcement.py` updated (assertion format: HTTPException wraps detail, no longer bare JSONResponse). Total: 1756 passed, 7 skipped. Gap register: 195 items, 169 complete, 87%.

**2026-08-15 session 17 (Final Engineering Gap-Closure Program — this session):** DATA-CONF-01 FIXED LOCALLY: `_data_freshness(db, scope, sq_id, wing_id)` helper added to `dashboard.py`; imported into `planning.py`. Squadron scope: unrecorded outcomes (past `Session.status` not in terminal set), CEA import age (`MAX(Activity.updated_at) WHERE cea_seq_nr IS NOT NULL`), incomplete facilitators (active with null/empty subject_areas). Wing/national: coverage_pct (% squadrons with delivered session in last 60 days). Both `/api/dashboard/charts` and `/api/planning/command-centre` return `data_freshness:{as_at, coverage_pct, issues}`. Frontend: `_renderDataConfBar()` + `data-conf-bar` element on dashboard page; amber warning bar with issue chips when issues present. AUTO-01 date collision in `test_planning.py` fixed (2027-05-07 → 2027-09-11, 2027-05-14 → 2027-09-18). 4 new DATA-CONF-01 regression tests in `test_planning.py`. Total: 1725 passed, 6 skipped. Totals: 161/194 = 83%.

**2026-08-15 session 16 (Final Engineering Gap-Closure Program — this session):** HOL-01 FIXED LOCALLY (previous context): `statutory_holiday` label added to `_HOL_TYPE_LABELS` dict in connected-frontend; 1 regression test `test_statutory_holiday_type_round_trips` added. FAC-DUP-01 FIXED LOCALLY (previous context): structured `#m-fac-dup` disambiguation modal in connected-frontend with 4 actions (Cancel, Use Existing, Create Different, Merge Records); MERGE reuses `/api/facilitators/{id}/absorb` backend endpoint. MAINT-02 FIXED LOCALLY (this context): full NORMAL→PENDING→LOCKED→NORMAL state machine. Backend: `_compute_phase()` helper in `main.py`; `maintenance_pending_until` SystemSetting key; `_maintenance_active()` returns 6-tuple with phase; `maintenance_gate` middleware passes PENDING through; GET `/api/system/maintenance` + `/api/health/ui-config` both return phase/pending_until/title; `MaintenanceIn` adds `title` + `drain_seconds` (default 20, cap 300); enable stores `pending_until`; disable clears it; `invalidate_maintenance_cache()` called on both. Frontend: `_pollMaintenanceStatus()` self-scheduling — 5 s during PENDING, 90 s otherwise; amber `⚠️ pending` CSS variant + red `🔒 locked` banner; System Console form gains title + drain_seconds fields; panel shows PENDING/LOCKED label + write-block status. Tests: 8 new MAINT-02 regression tests in `test_system_admin.py`; pre-existing 26 enforcement tests updated with `drain_seconds=0` to enter LOCKED immediately — all pass. Total: 1719 passed, 6 skipped (up from 1710 baseline). Totals updated: 159/194 = 82%.

**2026-08-15 session 19 (this session):** PN-WIZ-01 FIXED LOCALLY — guided 6-step session builder wizard added to `connected-frontend/index.html`. `#m-sess-wizard` modal with WHO→WHAT→WHEN→WHO WILL DELIVER→WHERE→WHAT IS NEEDED→CHECK flow; 7-dot progress indicator; Back/Next/Save footer; module-level `_wizState` state machine. Step 4 loads FAC-SUG-01 suggestions using any existing session on the PN as scope anchor; silent fallback when no prior session exists. "Add session" bar at bottom of PN detail (Quick entry toggle + Guided mode button) when `canWriteSquadron() && !closed`; `_qeSubmit()` handles inline quick entry. CSS: `.wiz-dot`, `.wiz-q`, `.wiz-hint`, `.wiz-curr-item`, `.add-sess-bar`, `.qe-form`. `_wizSave()` calls `POST /api/sessions` then `PUT .../audience` with `training_class_ids` (correct field name confirmed from backend). `test_pn_wizard.py`: 7 tests — all pass. Full suite: 1753 passed, 7 skipped (no regressions). Totals: 166/194 = 85%.

**2026-08-15 session 18 (this session, continued):** FAC-SUG-01 FIXED LOCALLY — `GET /api/sessions/{sid}/facilitator-suggestions?parade_date={date}&period={n}` endpoint added to `training.py`; deterministic scoring: +5 subject_area match, -1000 on leave, -500 conflict, -workload; ranked descending; returns `rank_label∈{SUGGESTED,AVAILABLE,CONFLICT,UNAVAILABLE}` + `reasons[]` + `conflicts[]` per facilitator. Frontend: `_loadFacSuggestions()` fires non-blocking on `quickEdit()` modal open; renders `.fac-sugg-panel` with ranked pills above `#qe-fa`; failure is always silent. CSS: `.fac-sugg-panel/.fac-sugg-item/.fac-sugg-pill` variants. 6 tests in `test_fac_suggestions.py` — all pass. Totals: 165/194 = 85%.

**2026-08-15 session 18 (this session, continued):** BULK-01 FIXED LOCALLY — `POST /api/parade-nights/bulk-apply-template` endpoint added to `training.py` with `_BulkApplyTemplateIn` Pydantic model; supports `conflict_resolution∈{skip_night,alongside,replace_draft}` and `dry_run`; replace_draft soft-archives planned sessions before applying; all applied sessions get `status=planned` (never copies delivered); batch_id UUID audited per night. Frontend: "Apply structure…" button in `pn-bulk-bar`; `m-bulk-apply-tpl` modal with template picker + conflict select + preview panel; `openBulkApplyModal()`, `_bulkApplyPreview()`, `_bulkApplyConfirm()` JS functions. 7 new regression tests in `test_pn_templates.py` — all pass (25 total). Totals: 164/194 = 85%.

**2026-08-15 session 18 (this session):** CLASS-FORECAST-01 FIXED LOCALLY — frontend component complete. `_loadClassForecasts(yearId)` function added to `connected-frontend/index.html`; fetches `GET /api/planning/class-forecasts?year_id={id}`; renders responsive `.fc-grid` of colored `.fc-card` elements (on-track/planning-risk/critical with green/amber/red top borders and status pills); hidden automatically for wing/national scope (403 path) or no classes. `py-forecasts-card` container added below Mission Backlog card; wired to year-selection via `loadYearMap()` alongside `loadMissions()`; cleared on year-deselect. CSS: `.fc-card`, `.fc-pill`, `.fc-stats`, `.fc-msg`, `.fc-grid` using AAFC design-token palette. Backend + 4 regression tests already complete (session 17). Totals: 163/194 = 84%.

**2026-08-15 session 15 (this session):** DASH-14, DASH-15, DASH-16 added and FIXED LOCALLY. DASH-14: "term comparison / YTD" PARTIAL item resolved — `_term_comparison_ytd()` stat-card in strategic chart bundle; `_chartTermYtd()` renderer; 5 tests. DASH-15: two Phase 6 items in one commit — (a) N/D rate explicit framing: `_delivery_trend` now emits `not_delivered_pct` + updated insight naming N/D rate; (b) deterministic forecast: `_delivery_forecast()` projects remaining planned sessions against YTD rate, labelled "deterministic — no AI scoring"; `_chartDeliveryForecast()` stat-card renderer; 5 tests. DASH-16: anomaly warnings — `_delivery_trend` flags weeks with delivery ≥20 pp below average (is_anomaly: true); `_chartLine` renders anomaly dots with orange stroke and larger radius. Backend: 1709 passed, 6 skipped. Commits `70cd49a`, `e5c0625`, `200faf3`. HELP-07 extended (commit `0f9c968`): Activity and CEA term added to KEY TERMS; Activities added as HOW TO GET STARTED step 5; "View Reports" and "Add an Activity" added to COMMON TASKS — addresses acceptance test item 12. DEF-27 added and FIXED LOCALLY: sqn_admin wing auto-select in Create Account modal (`_populateCreateWings()` condition extended from `S.isWing` → `S.isWing||S.role==='sqn_admin'`). CLASS-19 added and FIXED LOCALLY: "Class focus" filter in Planning Workspace calendar — chip section in `PlanningLeftPanel` (only shown for >1 class); `focusClassId` state in `PlanningWorkspace` dims non-matching session cells (opacity 0.22) in `ParadeNightBlock` via `EightWeekView`/`TermView`; commit `bd85d67`. CLASS-20 added and FIXED LOCALLY: CC endpoint joins `TrainingClass → CurriculumPhase` to emit `training_stage_id` + `stage_name`; `PlanningLeftPanel` groups class focus chips under stage sub-headers when multiple stages exist; single-stage UX unchanged; commit `ecff4fb`. WORK-19 added and FIXED LOCALLY: "Session search" input in `PlanningLeftPanel` — dims session cells not matching search text on title/curriculum code; `× Clear` button; same prop-threading as focusClassId; commit `a4e426c`. CLASS-21 added and FIXED LOCALLY: Foundation/Extension tier filter in PW — `CurriculumItem.core_status` emitted per session from backend (bulk pre-load, no N+1); `PlanningLeftPanel` gains "Session tier" All/Foundation/Extension chip row; `tierDimmed` logic composes with classDimmed/searchDimmed in `ParadeNightBlock`; `UnboundLocalError` in `get_annual_program` when year has no parade nights also fixed (`if all_pn_ids_pre and ts_rows:`); commit `b0e2273`. CLASS-22 added and FIXED LOCALLY: Stage focus filter in PW — "Stage focus" chip section (All + one chip per Training Stage) shown only when squadron has >1 Training Stage; `classStageMap` (Record<class_id, stage_id>) built in `PlanningWorkspace` from CC data; `stageDimmed` composes with classDimmed/searchDimmed/tierDimmed in `ParadeNightBlock`; commit `c62c678`. CLASS-23 added and FIXED LOCALLY: collapsible `ParadeNightBlock` — ▼/▶ chevron in block header toggles local `collapsed` state hiding notices/grid/warnings; `margin-left:auto` CSS pins button right; `stopPropagation()` prevents nav click; ARIA `aria-expanded`; commit `616a5c2`. CLASS-24 added as ACCEPTED RISK: program brief Phase 1.H "pinning where useful" deferred — the combination of class/stage focus, tier filter, search, and collapse (CLASS-19–23) adequately covers the usability need without explicit sticky-pin infrastructure; defer to Level B pilot feedback. Totals: 156/182 = 86%.

**2026-08-15 session 13:** DEF-23 (six save handlers lacked success toast) and DEF-24 (unhandled clipboard rejection in `Accounts.tsx`) — both FIXED LOCALLY. HARD-10 (cross-browser E2E coverage gap) added as NOT STARTED. DEF-23: `saveSessEdit`, `doSaveGettingHelp`, `saveRoom`, `saveEquip`, `doSaveSession`, `saveWingEvent` each now call `showToast()` on the success path; room/equip/event handlers distinguish create vs update in the message. DEF-24: `.catch(()=>{})` appended to `navigator.clipboard.writeText().then()` in `NewCodeModal`. Totals: 142/168 = 85%.

**2026-08-15 session 14 (this session):** HARD-10 promoted NOT STARTED → FIXED LOCALLY. Cross-browser Playwright projects (Chromium, Firefox, WebKit) confirmed working in `playwright.config.ts` and `playwright.connected.config.ts`. 30/30 auth.spec.ts tests pass across all three browsers. Root cause of earlier "Firefox failures" identified: SQLAlchemy connection-pool split state after live-server reseed, not a browser defect. Two date-collision time bombs fixed: `test_session_audience.py::_make_session` and `test_class_split_merge.py::_make_session` now use `date(2040, 1, 1)` anchor instead of `date.today()`, preventing collision with `test_planning.py`'s 2027-04-25 yearly parade night from ~2026-09-28. Backend: 1695 passed, 6 skipped (unchanged). DEF-25 added and FIXED LOCALLY: added Edit and Archive UI E2E tests to `facilitators.spec.ts` (commit `05435b6`); fixed "add" test to use `Date.now()` suffix for isolation; all 10 facilitator tests pass on Chromium, individual Firefox/WebKit pass. Cross-origin session handoff proven (HARD-10 remaining item): `cross-origin-session-handoff.spec.ts` 3/3 pass — TMS login sets `aafc_session` cookie; PW auto-authenticates without second login; TMS logout invalidates PW session (commit `b209d05`). CI extended to matrix across 3 browsers (6 parallel jobs). Totals: 144/169 = 85%.

**2026-08-14 session 12 (this session):** DEF-21 (dead-code constants with real personnel names) and DEF-22 (getCurrentUnitInfo S.cfg precedence bug) — both FIXED LOCALLY. F1: deleted 155-line dead-code block (CORE_CURR, DEFAULT_ACTS, DEFAULT_FACS, DEFAULT_ROOMS, DEFAULT_EQUIP, PN_703) from `connected-frontend/index.html`. F2: rewrote `getCurrentUnitInfo()` to prefer S.cfg backend values for operational time fields over SQN_DATA hardcoded values; also fixed secondary bug where addr was always returned as '' for the fallback branch. Totals: 140/165 = 85%.

**2026-08-14 session 11 (this session):** PARITY-02 added and FIXED LOCALLY — "Run checks" button added to `page-action-items` page header in `connected-frontend/index.html`; `runExceptionChecks()` calls `POST /api/exceptions/run-checks`, shows result toast, then `reloadAndRender()`; visibility gated by `canWriteSquadron()` in `renderActions()`. 6 backend regression tests added in `test_exceptions_run_checks.py` (unauthenticated 401, no-squadron returns 0, created count shape, facilitator-alert created for imminent session, idempotency, audit log). Backend tests: 1695 passed, 6 skipped. Totals: 138/163 = 85%.

**2026-08-14 session 8 (this session):** DEF-16, DEF-17, WORK-18 FIXED LOCALLY. DEF-16: `apiClosePN()` dead code wired — `doClosePN()` + "Close Parade Night" button in `showPNDetail()`; `close_blocked` surfaces per-session blockers from `e.body.detail.blockers`. DEF-17: facilitator leave conflict detection added to `_run_conflict_check` in `planning.py`; `facilitator_on_leave` added to `CONFLICT_TYPES`; 1 regression test. WORK-18: multi-select checkbox pattern on parade night list — `_pnSelected` Set, `pn-bulk-bar`, bulk Set Term + Archive actions via `Promise.all`. Backend tests: 1648 passed, 6 skipped. Total 125/159 = 79%.

**2026-08-14 session 10 (this session):** WORK-09 FIXED LOCALLY (commit `858fac0`) — migration v53 (`9997f6527ef4`) drops `planning_locations` + `scheduled_sessions`; `PlanningConflict.scheduled_session_id` FK converted to plain string; models removed from `planning.py` and `__init__.py`. DEF-07 + DOC-11 FIXED LOCALLY — `backend/tests/test_multi_wing.py` (16 tests): `POST /api/system/provision-wing` provisions synthetic 11WG; tests verify scope isolation (wing_admin denied 403 on national endpoints; cannot see 11WG data in wing-scoped reports; wing_id param bypass blocked) and national aggregation (national-overview + national-capability include both 7WG and 11WG). Backend: 1681 passed, 6 skipped. VIS-10 + DOC-03 FIXED LOCALLY — `frontend/e2e/multi-wing-scope.spec.ts` (14 Playwright tests): provisions 11WG in `beforeAll`; verifies national_admin sees both Wings in `/national-overview` UI and API; wing_admin sees only 7WG data; 403 on national endpoints; 401 unauthenticated. TypeScript: 0 errors. Totals: 133/160 = 83%.

**2026-08-14 session 9 (this session):** WORK-17 FIXED LOCALLY (commit `7e94ed9`) — `ParadeNightTemplate` + `ParadeNightTemplateSession` models added to `training.py`; migration v52 (`c3cb76ef2cf7`); 5 REST endpoints under `/api/parade-night-templates/`; 18 regression tests pass; frontend "Save as Tpl" + "Apply Tpl…" buttons on parade night cards with apply/delete modal. `e.detail` anti-pattern sweep completed: DEF-18 (`audienceErr.body?.detail`) + `publish_blocked` (`e.body?.detail`) both fixed; sweep confirmed all other `.detail` accesses in `connected-frontend/index.html` are correct. Backend test count: 1667 passed, 6 skipped. Totals: 127/160 = 79%.

**2026-08-14 session 8 (continued earlier):** DEF-16 added and FIXED LOCALLY — `apiClosePN()` was dead code; `doClosePN()` function and "Close Parade Night" button wired in `showPNDetail()`; `close_blocked` error now surfaces per-session blocker descriptions from `e.body.detail.blockers` via toast (bug fixed: original draft used `e?.detail` instead of `e?.body?.detail`; api() helper normalises error shape as `{kind,status,code,msg,body}`). DEF-17 added and FIXED LOCALLY — facilitator leave conflict detection wired into `_run_conflict_check`: `facilitator_on_leave` added to `CONFLICT_TYPES`, leave detection block queries `PlanningFacilitatorLeave` for all assigned facilitators overlapping the parade date, creates `warning` severity conflict per affected facilitator; 1 regression test added. Backend test count: 1648 passed, 6 skipped (confirmed). Total: updated above.

**2026-08-14 session 7 (this session):** Parade Night generation parity fix — `GuidedYearSetupModal.tsx` and `SetupPanel.tsx` in Planning Workspace now send `parade_start_time` and `parade_end_time` when entered (optional time inputs added to both UI forms); `index.ts` API type updated to include these fields. Previously the Planning Workspace generation path always used the squadron's configured default times regardless of operator input, while Main TMS presented explicit time pickers and forwarded the values. TypeScript: 0 errors. Registered as new gap PARITY-01, immediately FIXED LOCALLY. Sessions 5 and 6 merged into one as recorded audit (see below).

**2026-08-14 session 6:** Backend validation fixes, second pass (commit `3e97a50`): `_check_http_url()` module-level helper in `training.py` validates `learning_hub_url` in `CurriculumIn`, `CurriculumUpdateIn`, and `CurriculumImportItem` via Pydantic `field_validator` — blocks `javascript:`, `data:`, `vbscript:`, and any non-http/https scheme (stored XSS via curriculum write paths). Additional `max_length` constraints: `CurriculumIn.code` (40), `CurriculumIn.title` and `CurriculumUpdateIn.title` and `CurriculumImportItem.title` (200), `CurriculumImportItem.code` (40); `NightNoticeIn.priority` (20), `NightNoticeIn.audience` (60) in training.py; `NoticeIn.priority` (20), `NoticeIn.audience` (60), `NoticeUpdateIn.priority` (20), `NoticeUpdateIn.audience` (60) in planning.py; `TrainingClassIn.display_name` (80), `TrainingClassUpdateIn.display_name` (80). 7 new regression tests in `test_input_validation.py` (19 total — 18 passed, 1 skipped for test-DB data gap). Full suite: 1647 passed, 6 skipped.

**2026-08-14 session 5:** XSS sweep round 2 (commit `0ef96da`): `elBadge()` and `phBadge()` helpers wrapped with `esc()` — cascades to 12+ innerHTML call sites. Calendar chip direct phase fallback. Unit type badge. Facilitator profile modal (`facDisplay(f)`, `f.type`, subject-area tag fallback). `e.term` in curriculum list and detail modal. Audit log table `a.role`/`a.action`/`a.object_type`. React Planning Workspace confirmed clean (no `dangerouslySetInnerHTML`). Backend validation fixes (commit `d67c343`): input `max_length` constraints added to `FacIn`/`FacUpdateIn` (last_name/first_name=80, type=30), `ParadeIn`/`ParadeNightUpdateIn` (term=10), `CurriculumIn`/`CurriculumUpdateIn`/`CurriculumImportItem` (recommended_term=10); `Literal` type for `parade_type` (6 valid values → 422); `_VALID_SESSION_STATUS` set added to planning.py with 400 check in `update_session`; `audit()` added to `update_notice` (create/archive were audited; update was not). 12 regression tests in `test_input_validation.py`. Backend tests: 1641 passed, 5 skipped.

**Phase 7 typography sweep Round 2 (same session, later context):** Comprehensive sub-11px audit of both frontends. 30+ additional targeted changes: `.ssub` (→11px), `.badge` (→10.5px), `.nav-planning-hdr` (→10.5px), `.dash-sec-hdr` (→11px), `.ph-cnt` (→11.5px), `.chart-data-toggle` (→11.5px), `.wp-tbl th` Weekly Program print headers (→10.5px), `.clash-badge` (→11px), `.fac-sl` (→10.5px), `.sess-num` (→10.5px), `.sqn-chip` (→11px), `.exp-code` (→11px), `.lh-btn` (→11px), `.mode-banner button` (→11px), `.dash-filter-bar label` (→11px); "NEEDS ATTENTION" warning (→11px); "N classes need action" mission backlog alert (→11px); Weekly Program print header (→11px); NAT HQ/Wing source badges (→10px); facilitator schedule meta (→11px); parade-night section headers in detail panel (→11px); 8.5px inline badge sweep (→10px); curriculum class-status badges (→10px); unit type badge (→10.5px). Planning Workspace `planning.css`: 8 meta-label bumps (section headers, backlog codes, fac table headers, list-view period labels: all 10px→11px). Compact block/grid overrides (`pw-block-sm`, `pw-block-cg-*`) preserved. Deliberately excluded: calendar event chips (8–9.5px, layout-constrained), heatmap cells, pn-meta dark header footnote, cal-dn/cal-dt calendar grid elements. TypeScript: 0 errors. Backend unaffected.

---

## Status Definitions

| Status | Meaning |
|---|---|
| CLOSED | Gap fully addressed; evidence on record; backend + frontend verified, or gap N/A to frontend |
| STAGING VERIFIED | Working in staging; not yet confirmed in production |
| FIXED LOCALLY | Fix applied to codebase; not yet deployed or staging-verified |
| IMPLEMENTING | Partial implementation exists; specific remaining work stated in Current State |
| NOT STARTED | No implementation; gap confirmed real by direct code inspection |
| HUMAN GATE | Requires a non-engineering decision, approval, or manual action by a named authority |
| ACCEPTED RISK | Acknowledged; consciously not implemented for the current level; rationale on record |

---

## 1. Core Domain Architecture — Training Stage / Class

**Scope:** The full Training Class backend (migrations v42–v50, CRUD, audience, curriculum progress,
Mission Backlog class_breakdown, class-behind-threshold detection, 12 class-specific test files) is
CLOSED as of commit `afdc263`. All remaining CLASS-series gaps are in the frontend layer —
connected-frontend `index.html` and Planning Workspace React components. None of the backend
model additions removed any existing route or table; the capability manifest must be regenerated
to formally confirm this (see DOC-12).

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| CLASS-01 | Core Domain | HIGH | TrainingClass model: squadron_id, training_year_id, training_stage_id (FK curriculum_phases), display_name, sequence, soft-delete (SoftDeleteMixin), optimistic locking (version) | Migration v48; model in `training.py` alongside CurriculumPhase/Session/Cadet; follows PlanningYear's scoping shape | CLOSED |
| CLASS-02 | Core Domain | HIGH | SessionAudience join table: session_id, training_class_id, outcome_override, outcome_override_reason | Migration v49; many-to-many Session ↔ TrainingClass implemented; replaces one-value `Session.cadet_group` pattern | CLOSED |
| CLASS-03 | Core Domain | MEDIUM | CadetClassMembership: cadet_id, training_class_id, start_date, end_date (nullable), active_status, source, created_by | Migration v50; individual cadet-level membership tracking within a class | CLOSED |
| CLASS-04 | Core Domain | HIGH | TrainingClass CRUD endpoints — create, list, get, update, archive, restore | Full CRUD in `training.py`; 12 class-specific test files pass; 1553 tests passing overall | CLOSED |
| CLASS-05 | Core Domain | HIGH | TrainingClass split endpoint — reassigns members to a new class; historical class preserved as soft-archived, never hard-deleted | Implemented in `training.py` (split / reassign-members) | CLOSED |
| CLASS-06 | Core Domain | HIGH | TrainingClass merge endpoint — absorbs one class into another; historical class soft-archived | Implemented in `training.py` | CLOSED |
| CLASS-07 | Core Domain | HIGH | Session audience GET/PUT — `GET /api/sessions/{id}/audience` and `PUT /api/sessions/{id}/audience` with per-class outcome_override | Implemented; per-class outcome_override and outcome_override_reason exposed | CLOSED |
| CLASS-08 | Core Domain | HIGH | Curriculum progress per class — `GET /api/training-classes/{id}/curriculum-progress` using SUM(delivered)/SUM(applicable) per class, not average(percentages) | Implemented in `training.py`; aggregation method confirmed | CLOSED |
| CLASS-09 | Core Domain | HIGH | Phase-level class progress — `GET /api/curriculum/phases/{id}/class-progress` | Implemented in `training.py` | CLOSED |
| CLASS-10 | Core Domain | HIGH | Mission Backlog `class_breakdown` field — backend returns per-curriculum-item breakdown of which Training Class needs action | `planning.py` Mission Backlog API returns `class_breakdown` | CLOSED |
| CLASS-11 | Core Domain | MEDIUM | Class behind-threshold detection: backend flags classes >15 pp below their stage average | Backend detects and returns flag; 1553 tests passing including class-specific tests | CLOSED |
| CLASS-12 | Core Domain | HIGH | class_curriculum_progress chart in connected-frontend — chart container rendering per-class curriculum progress from `charts.class_curriculum_progress` | Confirmed implemented: `chart-class-curriculum-progress` container at index.html:792; rendered at index.html:6857 via `_dRenderChart`; insight at 6858. Gap register was stale. | CLOSED |
| CLASS-13 | Core Domain | HIGH | class_breakdown field rendered in connected-frontend curriculum page — per-item indication of which Training Class needs each curriculum item | Implemented as MBACK-01 (commit `16f6bce`): `_loadCurriculumClassBreakdown()` + `_currClassBreakdownHtml()` render per-class badges in curriculum page | CLOSED |
| CLASS-14 | Core Domain | MEDIUM | Training Classes step in the Getting Started checklist onboarding wizard | Confirmed implemented: `setup.py` lines 79-84 query TrainingClass count; lines 139-141 emit `training_classes_created` step. Gap register was stale. | CLOSED |
| CLASS-15 | Core Domain | LOW | CustomPhase → CurriculumPhase migration and cleanup — CustomPhase is a narrower, older squadron-scoped phase-name table that predates CurriculumPhase | Migration v40 (`5cf6d7e8f9a0`) written to DROP TABLE custom_phases; model class and `__init__.py` export removed; test updated; awaits operator `alembic upgrade head` to apply DROP TABLE on production | FIXED LOCALLY |
| CLASS-16 | Core Domain | LOW | Session.cadet_group / Cadet.phase formal deprecation — single-string fields replaced structurally by SessionAudience; all consumers (Mission Backlog, Weekly Program, dashboards, both frontends) must migrate before fields are removed | Deprecation plan at `docs/next-stage/28_cadet_group_deprecation_plan.md`: 73 total references across 4 files inventoried; 4-phase migration plan (dual-write → frontend selector → response deprecation → DB removal); Cadet.phase explicitly out of scope until individual cadet-class tracking is confirmed as a product decision | FIXED LOCALLY |
| CLASS-17 | Core Domain | MEDIUM | Training Class display and audience editing in Parade Night detail modal (`showPNDetail`) — the only session-editing path that previously had no Training Class support; quickEdit had checkboxes but the multi-session detail view was read-only and showed no class data | FIXED 2026-08-13: `_populatePnDetailClasses(pn)` added — fetches classes from `_tcById` cache (or live API if not populated), then per session loads current audience and renders checkboxes in `div#d-tc-${i}`. `savePNDetail()` updated to save audience per session after fields are saved; conflict detection (409 class_conflict) with inline Override link matches `saveSessEdit()` pattern. Read-only path (non-admin) shows current class names as plain text from `s.trainingClasses` (no extra fetch). | FIXED LOCALLY |
| CLASS-18 | Core Domain | LOW | Training Class names missing from compact session card — `buildPNCard()`'s `.sess-info` line showed Phase, Facilitator, and Room but not which Training Class the session targets; operators could not tell at a glance which class each card belongs to | FIXED 2026-08-13: `.sess-info` line now appends `· Class 1, Class 2` when `s.trainingClasses` is non-empty. Zero extra API calls — `trainingClasses` is already included in the parade night list response (`training.py` lines 308–325). E2E test added in `session-training-classes.spec.ts` (CLASS-18 describe block). | FIXED LOCALLY |
| CLASS-19 | Core Domain | MEDIUM | Planning Workspace calendar becomes unnavigable for squadrons with many parallel Training Classes — no way to focus on a single class's training without visual noise from all other classes | FIXED 2026-08-15: "Class focus" chip section added to `PlanningLeftPanel` (only shown when `trainingClasses.length > 1`). Selecting a chip sets `focusClassId` state in `PlanningWorkspace.tsx`; non-matching session cells in `ParadeNightBlock` receive `opacity: 0.22`; grid structure preserved (empty slots visible). Wired through `EightWeekView` and `TermView`. Commit `bd85d67`. TypeScript: 0 errors. E2E: `frontend/e2e/planning-workspace-filters.spec.ts` test 1 — clicks class chip, verifies `td[style*="opacity: 0.22"]` on non-matching session cells. Commit `fe5a757`. | FIXED LOCALLY |
| CLASS-20 | Core Domain | LOW | Class focus chips in PW not grouped by Training Stage — when a squadron has classes across multiple stages (e.g. 3 Senior + 2 Bronze), all chips render as a flat unsorted list with no visual grouping; operator cannot tell which stage a class belongs to at a glance | FIXED 2026-08-15: CC endpoint (`GET /api/planning/command-centre`) now joins `TrainingClass → CurriculumPhase` to emit `training_stage_id` and `stage_name` per class (one JOIN, no extra round-trip). `PlanningLeftPanel` groups chips under a stage sub-header ("Senior" / "Bronze") when `multiStage = stageOrder.length > 1`; sub-header suppressed for single-stage squadrons so the single-class UX is unchanged. Frontend type updated (`CommandCentreData.training_classes`). Commit `ecff4fb`. TypeScript: 0 errors. Backend: 1709 passed, 6 skipped. | FIXED LOCALLY |
| CLASS-21 | Core Domain | LOW | Planning Workspace has no Foundation / Extension curriculum tier filter — operator cannot dim sessions to see only Foundation (core) or Extension (additional curriculum) sessions; program brief Phase 1.H requirement. Note: Optional filtering deferred — `is_optional` does not exist on `CurriculumItem` (it is on `TimingBlock`); requires future Alembic migration to add the field before an Optional chip can be added. | FIXED 2026-08-15: `CurriculumItem.core_status` ("core"/"additional") used as the tier discriminator. Backend: `_real_session_out()` emits `core_status` per session; accepts pre-loaded `ci_tier` dict for bulk efficiency. `get_annual_program()` bulk-loads CI tier data in one query — guarded as `if all_pn_ids_pre and ts_rows:` (fixes `UnboundLocalError` when planning year has no parade nights). `get_long_range()` pre-loads CI tier for all parade nights in range before the per-date loop. Frontend: `NightSessionSummary` and `PlanningSession` types extended with `core_status?`; `DisplaySession` carries it; `fromNightSummary`/`fromPlanningSession` pass it through. `ParadeNightBlock` gains `tierFilter` prop; `tierDimmed`: "foundation" dims `core_status !== "core"`; "extension" dims `core_status !== "additional"`; composes with `classDimmed`/`searchDimmed`. `PlanningLeftPanel` gains "Session tier" chip section (All / Foundation / Extension). Wired through `PlanningWorkspace` → `TermView` / `EightWeekView` (all three variants). TypeScript: 0 errors. Backend: 1709 passed, 6 skipped. Commit `b0e2273`. E2E: `frontend/e2e/planning-workspace-filters.spec.ts` test 2 — creates squadron CI (core_status="additional"), verifies Foundation chip dims it; Extension chip does not dim it. Commit `fe5a757`. UnboundLocalError regression: `test_planner_v14.py::test_annual_program_empty_year_no_parade_nights` — empty year returns 200. Commit `fe5a757`. | FIXED LOCALLY |
| CLASS-22 | Core Domain | LOW | Planning Workspace has no Stage focus filter — when a squadron has Training Classes across multiple Training Stages (e.g. 3 Senior + 2 Bronze), there is no way to dim all non-Senior sessions at once; program brief Phase 1.H "Stage focus" requirement | FIXED 2026-08-15: `PlanningLeftPanel` gains "Stage focus" chip section (All / one chip per stage) shown only when `multiStage` (>1 distinct Training Stage). Stage chips derive from existing CC `training_classes` data (annotated with `training_stage_id`/`stage_name` from CLASS-20 — no extra API call). `PlanningWorkspace` builds `classStageMap` (Record<class_id, stage_id>) from CC data and passes it to TermView, EightWeekView (all three variants). `ParadeNightBlock` gains `focusStageId` + `classStageMap` props; `stageDimmed` dims sessions where none of the audience classes have `classStageMap[class_id] === focusStageId`; composes with `classDimmed`, `searchDimmed`, `tierDimmed`. TypeScript: 0 errors. Backend: 1709 passed, 6 skipped. Commit `c62c678`. E2E: `frontend/e2e/planning-workspace-filters.spec.ts` test 3 — creates 2 stages/2 classes; clicks Stage A chip; verifies Stage B session is dimmed. Commit `fe5a757`. | FIXED LOCALLY |
| CLASS-23 | Core Domain | LOW | Planning Workspace has no per-block collapse — operators with many parade nights or parallel Training Classes cannot reduce visual noise without leaving the view; program brief Phase 1.H "collapsing" requirement | FIXED 2026-08-16: collapse/expand toggle (▼ / ▶ chevron) added to every `ParadeNightBlock` header. Clicking the chevron hides the Notices band, period grid, and warnings footer; only the dark-blue header bar (date, fill count, conflict badge) remains visible. Local `collapsed` useState per block — no state lift required. Button uses `stopPropagation()` so the header nav click is not triggered. ARIA: `aria-expanded={!collapsed}` + accessible label. CSS: `.pw-block-collapse-btn` with `margin-left: auto` pins button to header right edge. TypeScript: 0 errors. Commit `616a5c2`. E2E: `frontend/e2e/planning-workspace-filters.spec.ts` test 4 — verifies `aria-expanded="true"` initially; click → `aria-expanded="false"` + `.pw-night-grid` not visible; click again → restored. Commit `fe5a757`. | FIXED LOCALLY |
| CLASS-24 | Core Domain | LOW | Program brief Phase 1.H lists "pinning where useful" as a Planning Workspace usability mechanism — sticky/pinned parade nights or classes visible regardless of scroll position | ACCEPTED RISK: The combination of class focus (CLASS-19/20), stage focus (CLASS-22), tier filter (CLASS-21), session search (WORK-19), and per-block collapse (CLASS-23) together provide sufficient navigation capability without explicit pinning. Pinning adds complexity (persistent state, visual affordance) for marginal gain over the existing filter+collapse toolkit. Deferred to Level B if the multi-class pilot identifies a concrete workflow that the existing features cannot serve. | ACCEPTED RISK |

---

## 2. Dashboard & Data Science

**Scope:** `dashboard.py` has ~35 chart/metric builder functions. Gap 10 (Tier 1 reports) and Wing
T2-04/T2-05 cards are CLOSED. The primary remaining gaps are frontend rendering of the new per-class
chart, metric documentation completeness, chart fault isolation, and accessible chart alternatives.
Backend dashboard builders are substantially complete; gaps are in documentation, verification, and
frontend consumption.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| DASH-01 | Dashboard | HIGH | Wing T2-04 (wing-cancellation-trend) and T2-05 (wing-not-delivered) wired to Wing dashboard cards | All 5 wing report endpoints rendered in connected-frontend; confirmed by `01_gap_matrix.md` Gap 10 closure | CLOSED |
| DASH-02 | Dashboard | HIGH | Zero-session parade night reports as `not_planned`, never as 100% ready | `dashboard.py` and `index.html:5672/:5713` correctly guard `sessions_total===0` / `planning_status==='not_planned'` | CLOSED |
| DASH-03 | Dashboard | MEDIUM | Full §23 metric dictionary pass — all 22 inventoried chart functions documented (QUESTION / PURPOSE / POPULATION / PERIOD / NUMERATOR / DENOMINATOR / SOURCE / REFRESH / ACTION / DRILL-DOWN) | Complete: `docs/product-review/dashboard-metric-dictionary.md` — 26 chart functions fully documented across Sections A–D; 5 issues identified including B2 `delivered_with_issue` exclusion (data integrity), equipment column always no_data, and "Reason not recorded" actionability gap | FIXED LOCALLY |
| DASH-04 | Dashboard | MEDIUM | Per-chart fault isolation in backend — each chart builder in `_full_squadron_charts` wrapped in its own try/except so one failure cannot 500 the whole `/api/dashboard/charts` response | `_run_chart_builder()` at `dashboard.py:62` wraps each builder in try/except, returning `chart_type="error"` on failure; confirmed implemented | CLOSED |
| DASH-05 | Dashboard | MEDIUM | Frontend chart error handling — catch handler resets all chart containers on failure, not just 2 of 7+ (noted at `index.html:5793`) | `_DASH_CHART_IDS` at `index.html:6797` now covers all 14 chart/insight pairs; both skeleton reset and fetch-failure cleanup use the shared list — implemented and commented at lines 6789–6796 | CLOSED |
| DASH-06 | Dashboard | LOW | Cancellation reasons "Unknown" / "Reason not recorded" — UI must surface an actionable prompt when this Pareto category is non-trivial, not display it as an inert bar | `loadDashCharts()` now appends a `a-warn` alert beneath the cancellation chart when the top reason is a `data_quality_gap` row and accounts for ≥15% of cancellations, explaining how to fix it | FIXED LOCALLY |
| DASH-07 | Dashboard | MEDIUM | Accessible chart alternatives — data tables or text summaries alongside every chart element for screen-reader access | `_chartAccessibleTable()` at `index.html:6758` generates a collapsible `<details>/<summary>` data table for every chart; called by `_dRenderChart()` at line 6774 — already applied to all 14 dashboard charts | CLOSED |
| DASH-08 | Dashboard | LOW | National/Wing readiness matrix (`_readiness_matrix`), risk forecast (`_risk_forecast`), and command metrics — frontend drill-down fully wired and verified end-to-end | Code-inspected 2026-08-13: `cmdDrillReadiness()` (line 10203) wired via onclick in `_renderReadinessMatrix()` — expands `cmd-drill-{scope}` panel with per-column breakdown; `_renderRiskTimeline()` has inline detail table (no separate drill needed); `_renderPareto()` wires `drillDashChart()` for cancellation reason drill-down; `_chartStackedBarH()` (immediate_issues) is informational only. All drill paths confirmed present. | FIXED LOCALLY |
| DASH-09 | Dashboard | MEDIUM | Curriculum progress per phase computed using governed CurriculumPhase catalogue (not hardcoded 8-phase list) | `_phases_for_squadron()` confirmed to read governed `CurriculumPhase` catalogue after earlier fix; hardcoded 8-phase list removed | CLOSED |
| DASH-10 | Dashboard | MEDIUM | Adoption analytics — per-squadron meaningful-activity summary for Wing/National view; shows active accounts, sessions scheduled, outcomes recorded, programs published, facilitators managed, last activity; period selector (7d/30d/term/year); bar chart for squadron comparison | FIXED 2026-08-13: `GET /api/dashboard/adoption` added to `dashboard.py`; aggregates `AuditLog` by squadron_id; 14 regression tests pass; "Section C — System Adoption" lazy-load table added to `loadCommandDashboard()` in connected-frontend with inline minibar chart; not a surveillance tool (squadron-level aggregates only, not per-user breakdown) | FIXED LOCALLY |
| DASH-11 | Dashboard | MEDIUM | Wing Assurance — "Cross-squadron not-delivered" report — `GET /api/reports/wing-not-delivered` was backend-complete (ops.py) but had no Planning Workspace frontend card; Wing command had no view of delivery failures across squadrons from the PW | FIXED LOCALLY 2026-08-13: `wingNotDelivered` added to `reportApi` and `WingNotDeliveredReport`/`WingNotDeliveredSqn` types added in `api/types.ts`; card added to `WingOverview` in `Overviews.tsx` — renders only when `total_not_delivered > 0`, shows squadron / count / curriculum codes table with `DecisionBadge`. `ReportCatalogue.tsx` updated from `frontend:"no"` → `frontend:"yes"`. TypeScript: 0 errors. | FIXED LOCALLY |
| DASH-12 | Dashboard | MEDIUM | Wing Assurance — "Cancelled / rescheduled trend" report — `GET /api/reports/wing-cancellation-trend` was backend-complete (ops.py, documented in ReportCatalogue) but had no Planning Workspace frontend; Wing command had no view of which squadrons had the most cancelled sessions/nights | FIXED LOCALLY 2026-08-13: `wingCancellationTrend` added to `reportApi` and `WingCancellationTrend`/`WingCancellationSqn` types added in `api/types.ts`; card added to `WingOverview` — renders only when total_cancelled_sessions + total_cancelled_nights > 0, shows squadron/cancelled-sessions/cancelled-nights/top-reason table. `ReportCatalogue.tsx` updated. TypeScript: 0 errors. | FIXED LOCALLY |
| DASH-13 | Dashboard | LOW | Phase 6 analytics — class enrollment distribution chart absent; no visual summary of expected cadet count per Training Class, making it impossible to check class balance or match room capacity at a glance from the dashboard | FIXED 2026-08-15: `_class_enrollment_distribution()` added to `dashboard.py`; queries `TrainingClass.expected_count` grouped by `CurriculumPhase.display_name`; chart_id `class_enrollment`; flags NULL expected_count as data quality gap in insight; `_full_squadron_charts` calls it; `_chartClassEnrollment()` renderer added to connected-frontend; chart card (hidden when no classes) added to Section B of squadron dashboard; 4 regression tests in `test_dashboard_class_enrollment.py` all pass (1699 passed, 6 skipped after addition). | FIXED LOCALLY |
| DASH-14 | Dashboard | LOW | Phase 6 analytics — term comparison and YTD delivery metric absent; `_long_term_delivery_trend` showed per-term reliability but provided no year-to-date rate and no current-vs-prior-term delta, so the question "Is delivery improving between terms?" had no direct answer on the dashboard | FIXED 2026-08-15: `_term_comparison_ytd()` added to `dashboard.py`; groups terminal sessions by term using latest-PN-date ordering (not alphabetical, to handle T4/T1 year transitions correctly); computes YTD from current-year sessions up to today; returns `ytd_pct`, `current_term`, `current_term_pct`, `prev_term`, `prev_term_pct`, `delta_pct`; wired into `_full_squadron_charts`, `_full_squadron_strategic_charts`, `_wing_strategic_charts`, `_national_strategic_charts`; `_chartTermYtd()` stat-card renderer added to connected-frontend showing three boxes (YTD / current term / prior term) with RAG colour and delta arrow; card hidden when no term data; 5 regression tests in `test_dashboard_term_comparison_ytd.py` all pass (1704 passed, 6 skipped). Commit `70cd49a`. | FIXED LOCALLY |
| DASH-15 | Dashboard | LOW | Phase 6 analytics — (a) N/D rate not explicitly framed: `_delivery_trend` returned `reliability_pct` only, making the non-delivery complement invisible; (b) deterministic delivery forecast absent: no end-of-year projection of sessions at risk based on current rate | FIXED 2026-08-15: (a) `_delivery_trend` data rows now include `not_delivered_pct` and `not_delivered` count; insight updated to name N/D rate explicitly when stable-but-high or declining; (b) `_delivery_forecast()` added: computes `ytd_delivered`/`ytd_not_delivered`/`delivery_rate_pct` from terminal sessions YTD, then `projected_delivered` = remaining planned × rate and `projected_missed` = remainder; wired into `_full_squadron_charts`; `_chartDeliveryForecast()` stat-card renderer (4 boxes: rate / remaining / projected deliver / at risk) with RAG colour; card hidden when no data; explicitly labelled "deterministic — no AI scoring"; 5 regression tests in `test_dashboard_delivery_forecast.py` all pass (1709 passed, 6 skipped). Commit `e5c0625`. | FIXED LOCALLY |
| DASH-16 | Dashboard | LOW | Phase 6 analytics — anomaly warnings absent: no mechanism to flag parade nights or weeks with delivery rates significantly below the squadron's own baseline | FIXED 2026-08-15: `_delivery_trend` now computes `anomaly_weeks` list — flags any week with ≥4 sessions where `reliability_pct < window_average − 20 pp`; annotates each row with `is_anomaly: true/false`; appends "Anomalous weeks detected: …" to insight when anomalies found. Frontend: `_chartLine` renders anomaly dots at `r=5` with orange stroke (vs standard `r=3` white-stroke) so they visually distinguish from normal data points; SVG title includes "⚠ anomaly" label. Commit `200faf3`. | FIXED LOCALLY |

---

## 3. Mission Backlog

**Scope:** Backend Mission Backlog API is CLOSED (returns `class_breakdown`, per-class progress,
and curriculum item breakdown). Frontend consumption gaps are the remaining work.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| MBACK-01 | Mission Backlog | HIGH | class_breakdown per curriculum item displayed in connected-frontend curriculum page — identifies which Training Class needs each item | Implemented commit `16f6bce`: `_loadCurriculumClassBreakdown()` fetches mission backlog on curriculum page nav; `_currClassBreakdownHtml()` renders per-class badges (b-ok/b-blue/b-amber/b-red by status). | FIXED LOCALLY |
| MBACK-02 | Mission Backlog | MEDIUM | Per-class Mission Backlog filtering in UI — view backlog filtered to a single Training Class | `mission-filter-class` select populated dynamically from `class_breakdown` in `loadMissions()`; `renderMissions()` filters to missions where selected class is not resolved — confirmed present in `index.html:10485–10522` | CLOSED |
| MBACK-03 | Mission Backlog | MEDIUM | Needs Attention consolidated queue (§39) — single prioritised surface combining readiness warnings, class gaps, and required planning actions | `renderActions()` at `index.html:9371` consolidates: backend action items (P0/P1), past sessions with unrecorded outcomes (P2), cancelled/ND with no reason (P3), unassigned sessions (P4); accessible as `page-action-items` in nav ("Needs Attention"). Commit `258e38c`. Gap register was stale. | CLOSED |
| MBACK-04 | Mission Backlog | LOW | Plan-faster shortcut from Mission Backlog item directly to a session slot (§41) | `navToScheduledPN(date)` added: navigates to Parade Nights page and pre-fills the search filter with the scheduled date. "↗ View PN" button appears in the Scheduled cell for any scheduled mission. Also fixed stale `pn-date-filter` reference in the Cmd+K command palette (should be `pn-search`). E2E test added 2026-08-13: `low-severity-fixes.spec.ts` (MBACK-04 test waits for `button[title='Go to this parade night']` after navigating to activities — relies on seeded 2026 planning year data for squadron 703). | FIXED LOCALLY |
| MBACK-05 | Mission Backlog | MEDIUM | Per-class conflict detection — when assigning a Training Class to a session audience, detect class-schedule clashes (same class in two concurrent sessions at the same parade night + period) | Backend: `SessionAudienceSetIn.override_conflict: bool = False`; `set_session_audience()` checks sibling sessions; raises 409 `{"error":"class_conflict","conflicts":[...]}`. Frontend 2026-08-13: `saveSessEdit()` now catches 409 class_conflict from audience save; displays named conflicting classes in `sess-edit-err` with an inline Override link; Override retries `_saveSessionAudience()` with `override_conflict:true`. Session fields are saved atomically before the audience check, so only the audience step can trigger the conflict warning. | FIXED LOCALLY |
| MBACK-06 | Mission Backlog | LOW | Per-class delivery summary — ability to export a per-class delivery record (what each Training Class has received, by curriculum item) as a report or CSV | Implemented commit `ca15a86`: `exportPerClassDeliveryCSV()` in connected-frontend pivots `_missionState.missions[].class_breakdown` into a CSV (Class, Code, Title, Stage, Element, Recommended Term, Delivery Status, Sessions Scheduled), one row per (Training Class × curriculum item). Button "Per-Class CSV" in Mission Backlog filter bar. Download via Blob. E2E test added 2026-08-13: `low-severity-fixes.spec.ts` verifies button is visible after Mission Backlog loads. | FIXED LOCALLY |

---

## 4. Help & First-Time Experience

**Scope:** No Help Centre, Glossary, or contextual help feature exists in either frontend. The
Getting Started wizard (`setup.py`) exists but is incomplete for Training Classes. "Getting Help"
is an empty-string admin-editable text field — no default content ships.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| HELP-01 | Help | MEDIUM | Contextual help tooltips on key fields across both frontends — hover/focus reveals a plain-language explanation of each field's purpose | CSS `.ht` tooltip class added; 11 tooltips on key fields in connected-frontend: Training Stage (add/edit class modals), Display Name, Order, Facilitator Type, Current Rank, Account Role, Delivery reliability metric, Mission Backlog title. Hover or keyboard-focus reveals a brief explanation. E2E test added 2026-08-13: `low-severity-fixes.spec.ts` verifies `.ht` button presence and non-empty `data-tip` in Mission Backlog header. | FIXED LOCALLY |
| HELP-02 | Help | LOW | Glossary of terms accessible from within the application — defines Training Stage, Training Class, Parade Night, Curriculum Item, Mission Backlog, and other domain terms | `HelpDrawer.tsx` Glossary tab has 14 accordion-expanded definitions; updated 2026-08-13: Training Class definition now explains WHY Stage and Class differ and gives concrete multi-class progress example; Session definition now explains combined sessions (one session serving multiple classes). `_GH_DEFAULT` (connected-frontend Getting Help default text) also expanded with same concepts. | CLOSED |
| HELP-03 | Help | MEDIUM | Help Centre 17-question acceptance test — all questions a new Training Officer would ask are answerable from within the application, without external reference | `HelpDrawer.tsx` answers all 17 questions across Overview, Tasks (9 accordion guides including new "Schedule one session for two or more Training Classes"), Glossary (14 terms), and Support. Updated 2026-08-13: "Schedule a training session" step updated to say "one or more Training Classes"; new combined-session task added. | CLOSED |
| HELP-04 | Help | MEDIUM | Pre-flight / readiness check UI — explainable per-item checklist (✓ / ! per item) rather than a single blended readiness percentage | `_renderTonightReadiness()` now renders a "Readiness checklist" section below the session list showing Sessions planned (b-ok badge), Facilitators assigned (b-ok/b-amber/b-red with N/M count), Rooms assigned (same) — derived from `d.fac_filled`, `d.room_filled`, `d.sessions_total` already returned by `_tonight_readiness` backend. E2E test added 2026-08-13: `low-severity-fixes.spec.ts` (HELP-04 test seeds a parade night for today's date + one session, then navigates to dashboard and waits for `#dash-tonight-section` to contain "Readiness checklist"). | FIXED LOCALLY |
| HELP-05 | Help | LOW | "What changed?" view (§40) — changelog or activity feed showing recent modifications to the current week's plan | Implemented commit `f9ebfcb`: `GET /api/recent-changes` (ops.py) returns scoped audit-log entries for planning object types; "What Changed?" card appended to Needs Attention page with date selector (1–30 days) and relative timestamps; 4 tests added. E2E test added 2026-08-13: `low-severity-fixes.spec.ts` verifies "What Changed?" card, `#rc-feed`, and `#rc-days` are present on the Needs Attention page. | FIXED LOCALLY |
| HELP-06 | Help | LOW | Universal search / command palette (§6) — single-keystroke access to any entity, page, or action from anywhere in the application | `openCmdPalette()` at `index.html:12890`; toolbar button and ⌘K/Ctrl+K shortcut; accessible from all squadron/wing/national scopes. Commit `c004b29`. Gap register was stale. | CLOSED |
| HELP-07 | Help | LOW | Getting Help panel — admin-editable free text must have meaningful default guidance content for a new squadron's first session | FIXED 2026-08-13: `_GH_DEFAULT` JS constant (connected-frontend `index.html`) provides comprehensive default content rendered whenever the backend returns an empty string — covers TMS vs Planning Workspace, key terms (Training Year, Training Stage, Training Class with multi-class and combined-session explanation, Parade Night, Session, Mission Backlog), first-time setup steps, common tasks, and support pathway. Admin can override it at any time; the default is never lost (fallback always in frontend code). Extended 2026-08-15 (commit `0f9c968`): Activity and CEA term added to KEY TERMS (addresses acceptance test item 12 — where Activities come from); HOW TO GET STARTED step 5 added for Activities; "View Reports" and "Add an Activity" tasks added to COMMON TASKS section. | FIXED LOCALLY |
| HELP-08 | Help | MEDIUM | Setup status endpoint surfaced in UI — `GET /api/setup/status` exists but no setup-status summary is shown to a wing_admin or sqn_admin who has just completed Getting Started | `page-getting-started` renders `GET /api/setup/status` response via `loadGettingStarted()` at `index.html:4462`; accessible from nav as "Getting Started" for squadron/wing/national/system_admin scopes | CLOSED |

---

## 5. Workflow Efficiency

**Scope:** No click/field count baselines have been measured for any workflow. Three restore-UI
gaps affect four entity types. Bulk-import and smart-planning features are Level B/C items.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| PARITY-01 | Parity | LOW | Parade Night generation — Planning Workspace `generate-parade-dates` call missing `parade_start_time` and `parade_end_time` vs Main TMS which collects and sends both fields | Fixed 2026-08-14: `index.ts` type extended; time inputs added to `GuidedYearSetupModal.tsx` (DatesStep) and `SetupPanel.tsx`; fields sent as optional overrides; omitted when blank, falling back to squadron default on the backend. TypeScript: 0 errors. | FIXED LOCALLY |
| PARITY-02 | Parity | LOW | `POST /api/exceptions/run-checks` — Planning Workspace has an automated "Run checks" button (ActionItems.tsx:15) that scans upcoming sessions for missing facilitators/rooms and creates action items; Main TMS Needs Attention page had only manual add/close, no automated check trigger | Fixed 2026-08-14: "Run checks" button added to `page-action-items` page header in `connected-frontend/index.html`; `runExceptionChecks()` async function calls `POST /api/exceptions/run-checks`, shows result toast (count or all-clear message), then calls `reloadAndRender()`. Button visibility gated on `canWriteSquadron()` and revealed by `renderActions()`. | FIXED LOCALLY |
| WORK-01 | Workflow | LOW | Click/field count baseline measured for all 12 high-frequency workflows (§37) so improvements can be measured | Fully measured 2026-08-13: `workflow-map.md` populated with steps/required fields/clicks for all 12 workflows; parity gaps and key findings documented | FIXED LOCALLY |
| WORK-02 | Workflow | MEDIUM | Restore UI for curriculum items — soft-archived via `is_archived=True` but no restore endpoint or UI control exists in either frontend | `POST /api/curriculum/{cid}/restore` at `training.py:3933`; `doRestoreCurrItem()` at `index.html:7819` shows Restore button in archived rows | CLOSED |
| WORK-03 | Workflow | MEDIUM | Restore UI for facilitators — soft-archived but no restore endpoint or UI control in either frontend | `POST /api/facilitators/{fid}/restore` at `training.py:1715`; `doRestoreFacilitator()` at `index.html:8787`; "Show archived" checkbox with Restore button per row | CLOSED |
| WORK-04 | Workflow | MEDIUM | Restore UI for Wing HQ calendar events and anchor events — soft-archived but no restore endpoint or UI | Wing HQ events: fully implemented — `doRestoreWingEvent()` at `index.html:12710`. Anchor events: `POST /api/planning/anchors/{id}/restore` added (commit `aaf3a2d`→next); `include_archived` param added to list endpoint; `doRestoreAnchor()` added to frontend; anchor tab hidden from pilot nav but backend+JS ready | FIXED LOCALLY |
| WORK-05 | Workflow | LOW | Flexible time blocks within a parade night (§11/§12) — variable-length training blocks, not only fixed-template slots | Implemented commit `46c72ea`: "Override Parade Night Timing" modal extended with mode toggle — "Use existing template" (unchanged) or "Custom blocks for this night" (inline block editor). On save in custom mode, auto-creates a named template then applies it as override. No migration needed. **Bug found 2026-08-13:** `openPNTimingOverrideModal()` was defined but never called — the modal was unreachable by operators. Fixed: "Timing…" button added to `buildPNCard()` header for `canWriteSquadron()` users (same pattern as "Copy to…"); triggers `openPNTimingOverrideModal('${pn.id}')`. E2E test added to `parade-night-bulk-ops.spec.ts` (WORK-05 test verifies "Timing…" button visible and modal opens with both mode toggle buttons). | FIXED LOCALLY |
| WORK-06 | Workflow | LOW | Smart planning assistance — when building a session, suggest under-covered curriculum items for the attending class (§13) | Planning Workspace curriculum combobox sorts unscheduled and reschedule-needed items to the top; reschedule-needed (orange) and needs-scheduling (blue) indicators; results limit raised from 10 to 12. Commit `5171525`. Gap register was stale. | CLOSED |
| WORK-07 | Workflow | MEDIUM | Bulk holiday import with explicit `holiday_type` field (§41) — if built, must not silently default to `school_holiday`; no holiday import path exists today | `export_import.py` has zero references to `holiday_type`; flagged as design constraint for future import work | ACCEPTED RISK |
| WORK-08 | Workflow | LOW | Session move by drag-and-drop or equivalent shortcut within Planning Workspace | FIXED 2026-08-13: "Move to another night" collapsible section added to `SessionForm` in `PlanningRightDrawer.tsx`; uses `trainingApi.editSession()` (PUT `/api/sessions/{id}`) which accepts `parade_night_id` + `period_number`; parade night selector populated from `planningApi.nightSummaries()` filtered to nights with a `parade_night_id`; lazy-loads only when section is expanded; invalidates `planning-weekly`, `planning-long-range`, `planning-annual`, `planning-cc` queries on success. E2E test added 2026-08-13: `frontend/e2e/session-drawer-move.spec.ts` (verifies "Move to another night" button is visible in PW right drawer for sqn_admin after clicking an existing session cell in the Night view grid) | FIXED LOCALLY |
| WORK-09 | Workflow | MEDIUM | PlanningLocation Phase 2 table drop — `planning_locations` is inert (no new writes since canonical model decision), but the table still exists and the fallback resolver still queries it | Decision in `04_canonical_data_model.md`; Phase 2 complete: migration v53 (`9997f6527ef4`) drops `planning_locations` + `scheduled_sessions`; `PlanningConflict.scheduled_session_id` FK converted to plain string; models removed from `planning.py` and `__init__.py`; 1681 tests pass. Commit `858fac0`. | FIXED LOCALLY |
| WORK-11 | Workflow | LOW | Copy parade night sessions — operator can reuse the planning structure of a previous parade night (curriculum items, facilitators, rooms, Training Class audiences) for a future night without re-entering each session | FIXED 2026-08-13: `POST /api/parade-nights/{pnid}/copy-sessions-to/{target_pnid}` in `training.py`; copies planning fields only (not outcomes, not publication state); skips periods already occupied in the target (returns {copied:N, skipped:N}); 8 tests in `test_pn_copy_sessions.py` pass. Frontend: "Copy to…" button on each parade night card opens a modal with a sorted target-night selector; `openCopyPNModal()`/`doCopyPNSessions()` in `index.html`; success toast shows copied count and skipped note. | FIXED LOCALLY |
| WORK-10 | Workflow | MEDIUM | Publish Weekly Program parity — Planning Workspace has a Publish button; Main TMS had `apiPublishPN()` defined but wired to no UI control (dead code); operators using Main TMS could not publish | FIXED 2026-08-13: `publishWP()` function added to connected-frontend; Publish Program button added to renderWP() button bar (admin users only); handles publish-blockers 409 with actionable error message; `pn.published` already in S.pns state. E2E test added 2026-08-13: `weekly-program-classes.spec.ts` (WORK-10 test verifies button is visible to sqn_admin after selecting a parade night date) | FIXED LOCALLY |
| WORK-12 | Workflow | LOW | Bulk cancel all remaining planned/published sessions in a parade night — operator needs a single action to cancel every in-flight session (with a reason) rather than clicking Save Changes on each one individually; mirrors mark-remaining-delivered for the cancellation use case | FIXED 2026-08-13: `POST /api/parade-nights/{pnid}/cancel-all` in `training.py` (`CancelAllIn` body with `reason`/`notes`; status filter `draft|planned|published`; per-session `_apply_status_transition()` + `SessionStatusHistory` + `audit()` with shared `batch_id`; returns `{ok, batch_id, sessions_updated, session_ids}`). Frontend: `btn-red-out` CSS class added; "Cancel all remaining" button in parade night detail header; `doCancelAllSessions()` calls `collectOutcomeReason('cancelled')` then POSTs and toasts result. 12 tests in `test_pn_cancel_all.py` all pass (auth, 403, 404, 422, empty pn, happy path, response shape, skips finalised, skips delivered, idempotency, status history, audit). Backend: 1618 passed, 5 skipped. | FIXED LOCALLY |
| WORK-13 | Workflow | LOW | Filter persistence — parade night filters (term, status, search) and mission backlog filters (phase, status, search, class) reset to defaults on page refresh; operator must re-apply filters after every refresh or reboot | FIXED 2026-08-13: `_savePNFilters()`/`_restorePNFilters()` and `_saveMissionFilters()`/`_restoreMissionFilters()` in `index.html`; write current filter values to `sessionStorage`; `bootApp()` calls both restore functions before `renderAll()`; "Clear filters" also removes saved PN state. No operational data stored — UI preference strings only. E2E tests added 2026-08-13: `filter-persistence.spec.ts` — 2 tests verify pn-f-status and pn-f-term persist through `page.reload()` via the sessionStorage round-trip. | FIXED LOCALLY |
| WORK-14 | Workflow | LOW | Export Annual Program to XLSX — `exportAnnualProgram()` was fully implemented (JS + backend `GET /api/planning/years/{id}/export.xlsx`) but never wired to a UI button; operators could not reach the export | FIXED LOCALLY 2026-08-13: "Export Annual" button added to `_renderPyActionBtns()` in `connected-frontend/index.html` — appears alongside Rename/Archive/Delete when a Training Year is selected; no `plan-write-el` guard so all roles with planning access can export. E2E test added: `planning-exports.spec.ts` (WORK-14 test verifies button visible after year selection). | FIXED LOCALLY |
| WORK-15 | Workflow | LOW | Export Schedule to XLSX — `exportSchedule()` was fully implemented (JS + backend `GET /api/planning/years/{id}/schedule/export.xlsx`) but never wired to a UI button; operators could not reach the schedule export | FIXED LOCALLY 2026-08-13: "Export Schedule" button added to `_renderPyActionBtns()` in `connected-frontend/index.html` alongside "Export Annual". E2E test added: `planning-exports.spec.ts` (WORK-15 test verifies button visible after year selection). | FIXED LOCALLY |
| WORK-16 | Workflow | MEDIUM | Bulk facilitator assignment — operator cannot assign the same facilitator (or clear a facilitator) across multiple sessions in a parade night at once; must edit each session individually | FIXED 2026-08-14: "Quick Assign to All Sessions" bar added to parade night detail modal in `connected-frontend/index.html` (between Parade Night Details card and session cards); `_qaApplyAll(n)` sets facilitator and/or room on all `d-fa-N`/`d-rm-N` selects; `_qaClearFacs()` blanks all facilitator selects; existing "Save Changes" button commits all values at once. Only shown for `canWriteSquadron()` and ≥2 sessions. No backend change required. | FIXED LOCALLY |
| WORK-17 | Workflow | LOW | Parade night session template — operator cannot save a parade night's full session structure (curriculum items + facilitators + rooms per period) as a named reusable template to apply to a blank future night | `ParadeNightTemplate` + `ParadeNightTemplateSession` models (migration v52, revision `c3cb76ef2cf7`); `POST /api/parade-nights/{pnid}/save-as-template`, `GET /api/parade-night-templates`, `GET /api/parade-night-templates/{tid}`, `POST /api/parade-night-templates/{tid}/apply/{pnid}`, `DELETE /api/parade-night-templates/{tid}`; 18 regression tests. Frontend: "Save as Tpl" + "Apply Tpl…" buttons on parade night cards; apply modal lists templates with apply and delete per row. Route prefix `/api/parade-night-templates` (not under `/parade-nights/templates`) to avoid FastAPI wildcard shadowing by existing `{pnid}` route. | FIXED LOCALLY |
| WORK-18 | Workflow | LOW | Apply action to selected parade nights — no multi-select checkbox pattern on the parade night list; all actions (set term, set type, assign facilitator, etc.) must be applied one night at a time | `_pnSelected` Set at module level persists across `renderPN()` rebuilds; checkbox per `pn-card` (write-role only); `pn-bulk-bar` appears when ≥1 selected; actions: Set term (`PATCH /api/parade-nights/{id}` via `Promise.all`), Archive selected (`DELETE /api/parade-nights/{id}` batch); `_pnBulkUpdate()` shows count + shows/hides bar; `_pnClearSelection()` clears state and re-renders | FIXED LOCALLY |
| WORK-19 | Workflow | LOW | Planning Workspace calendar has no session search — operator cannot filter the calendar to show only sessions matching a curriculum item or keyword; must scan all session cells manually even in a full-term view | FIXED 2026-08-15: "Session search" text input added to `PlanningLeftPanel` left panel. Typing dims session cells whose `title` and `code` don't contain the search string (case-insensitive substring match). A "× Clear" button appears while text is present. Same prop-threading pattern as `focusClassId`: `searchText` state in `PlanningWorkspace` → `EightWeekView` / `TermView` → `ParadeNightBlock.searchText` (combined with `classDimmed` into `isDimmed`). Commit `a4e426c`. TypeScript: 0 errors. | FIXED LOCALLY |
| WRITE-12 | Defence Writing | MEDIUM | Error message and empty-state content — 60+ backend error codes untranslated; 15+ user-visible messages expose internal jargon ("Session ID", "in scope"), use passive voice, or give no actionable next step | FIXED 2026-08-13: (A) 18 new named-code translations in `api()` helper: `duplicate_date`, `parade_night_closed`, `already_archived`, `not_archived`, `planning_year_already_exists`, `cross_squadron`/`cross_squadron_copy_not_permitted`/`cross_squadron_move_forbidden`, `publish_blocked`, `close_blocked`, `invalid_training_class`, `training_class_wrong_squadron`, `training_stage_not_found`, `training_year_not_found_for_squadron`, `reason_required`, `name_required`, `name_exists`, `source_not_found`, `target_not_found`/`target_parade_night_not_found`. (B) 15 message rewrites: load-failure hides internal endpoint names; Session-ID-missing jargon removed; "in scope" → plain language; version conflict uses em dash; "Error loading X" → "Could not load X — Refresh the page"; "From"/"To" date validation → plain English; timing-override reason prompt; facilitator save toasts remove noisy timestamps; Mission Backlog export messages actionable; Training Year missing messages include dropdown context; wing-select error states actionable. | FIXED LOCALLY |

---

## 6. Defence Writing

**Scope:** All Defence Writing fixes are applied to codebase (FIXED LOCALLY status). None has been
formally staging-verified as a separate post-deploy check. Source: 46-row
`interface-language-inventory.csv` (all items reviewed; 35+ fix-applied or reviewed-no-change),
`defence-writing-ui-standard.md` (Manual §2.4–§5.82 translated to UI rules), and
`ui-copy-review-checklist.md`.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| WRITE-01 | Defence Writing | MEDIUM | All jargon / software terms removed from normal-user copy — `backend`, `endpoint`, `payload`, `schema`, `UUID`, raw HTTP codes — across both frontends (Manual §2.13.g) | 36 strings reviewed; 29 fix-applied: network errors, audit log subtitle, account management banners, planning error messages, reports page explanation, scope bar footnote, access-codes card, load-error hints | FIXED LOCALLY |
| WRITE-02 | Defence Writing | MEDIUM | Unambiguous date formats throughout — `DD Mon YYYY` or `DD Mon YY` only; numeric `DD/MM/YY` and `MM/DD/YY` forms prohibited (Manual §5.72) | 6 call sites fixed: Weekly Program footer, facilitator duplicate-warning card, Accounts last-login (Planning Workspace), Audit time column (Planning Workspace), facilitator card (Planning Workspace) | FIXED LOCALLY |
| WRITE-03 | Defence Writing | MEDIUM | 24-hour time for all operational time display — `1830` not `6:30 PM`; explicit `hour12:false` in all `toLocaleString` calls (Manual §5.79–5.80) | Planning Workspace Audit time column corrected to explicit `hour12:false` and unambiguous date format; no other 12-hour violations found | FIXED LOCALLY |
| WRITE-04 | Defence Writing | LOW | "Given name" / "Family name" — not "First name" / "Surname" — for all personal-name form fields in both frontends (Manual §3.34) | Both frontends already use correct labels. One stale error message "First and last name are required" in `PlanningBottomDrawer.tsx:861` updated to "Given name and family name are required" | FIXED LOCALLY |
| WRITE-05 | Defence Writing | LOW | No contractions in formal copy — `cannot` not `can't`; `will not` not `won't`; `you would` not `you'd` (Manual §2.13.h) | 6 instances fixed via multi-line Python sweep (archive confirmations, scope bar, unit settings, delete toasts, Getting Started subtitle, Unit Settings parade-day explanation); sweep confirmed no remaining instances | FIXED LOCALLY |
| WRITE-06 | Defence Writing | LOW | Australian English spelling throughout — `organise`, `colour`, `program` (single m) (Manual §3.4, §3.15) | No violations found in targeted grep sweep; confirmed clean | FIXED LOCALLY |
| WRITE-07 | Defence Writing | LOW | Active voice throughout — subject-verb-object; passive permitted only where a deliberate technical/objective register is required (Manual §2.53–2.58) | Applied in rewrites for error messages, confirmation dialogs, and network error copy | FIXED LOCALLY |
| WRITE-08 | Defence Writing | MEDIUM | Confirmations for irreversible actions state what changes, what stays, and whether the action can be undone — hard-delete must say "cannot be undone"; soft-archive must say "records are preserved" (Manual §2.14, checklist §25) | Hard-delete confirmations (parade dates, holidays) updated with "cannot be undone"; soft-archive confirmations (parade nights, activities, training areas, equipment) updated with "records are preserved"; cascade blast-radius (all sessions on a parade night) explicitly named | FIXED LOCALLY |
| WRITE-09 | Defence Writing | MEDIUM | Error messages follow WHAT HAPPENED / WHAT IT AFFECTS / WHAT TO DO NEXT; empty states distinguish FAILED TO LOAD from NO DATA from NOT CONFIGURED (checklist §23/§24) | Applied to TermView, YearView, ParadeNightGridView, PlanningBottomDrawer, ListView (ListView previously displayed a failed fetch as NO DATA — real logic gap fixed, not copy-only) | FIXED LOCALLY |
| WRITE-10 | Defence Writing | LOW | Platform-neutral, accurate copy in maintenance and system notices — no stale hosting-platform names in any user-visible string | Staging cold-start banner corrected from "Render's free tier" to platform-neutral phrasing; `ENVIRONMENT` reference corrected to Railway | FIXED LOCALLY |
| WRITE-11 | Defence Writing | LOW | Defence Writing Manual Chapter 4 (Punctuation), 6 (Document presentation), 14 (Projection), 23 (Editing), 24 (Publications) — paragraph-level citation pass applied to all TMS UI copy and help text | FIXED 2026-08-13: paragraph-level active-voice pass applied — 7 confirmation dialog strings converted from passive future ("will be moved/archived/removed/unassigned") to active voice ("moves/archives/disappears"). Redundant "currently" removed from error and empty-state messages. Overly complex merge-modal text split into two shorter sentences. Facilitator-merge warning and Training Class merge confirm dialog simplified to ≤25 words per sentence. 2 HTML modal texts updated (merge Training Class body text, facilitator duplicate-merge warning). Manual §§2.53–2.58 (active voice), §2.13 (plain language), §2.14 (concise sentences) applied. | FIXED LOCALLY |

---

## 7. P0 / P1 Functional Defects

**Scope:** Confirmed defects or architectural gaps that block correct operation for real user journeys.
Items marked P0 are potentially showstopping for V1 if their scope is confirmed wider than currently
known. Firefox (DEF-01) and the Planning Workspace 404 (DEF-02) are both CLOSED — fixes confirmed by code inspection.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| DEF-01 | Defect | P0 | Firefox authentication — Planning Workspace cross-origin session handoff works in Firefox | Confirmed implemented: `index.html:4347-4354` — FF-01 fix already in place; uses `#t=<token>` hash fragment handoff bypassing SameSite=None cookie (which Firefox ETP blocks). `main.tsx` reads hash before React renders. Gap register was stale. | CLOSED |
| DEF-02 | Defect | P0 | Planning Workspace unexpected 404 — specific navigation path causes an unhandled 404 | Fixed commit `cb4a428`: stale `localStorage` year ID guard at `PlanningWorkspace.tsx:244-250` shows loading state instead of firing API calls with a non-existent `year_id`; 404 flash eliminated. Also fixed `sqn_general` session-read forbidden. Gap register was stale. | CLOSED |
| DEF-03 | Defect | P1 | Planning Workspace "Your scope" card — displays raw Wing ID and Squadron ID UUIDs instead of Wing code / name | `SessionInfo` in `api/types.ts` has `wing_code` / `squadron_code`; `Admin.tsx:17-18` uses `session.wing_code` / `session.squadron_code` directly — confirmed correct | CLOSED |
| DEF-04 | Defect | P1 | Facilitator rank stored as uncontrolled free text (`Facilitator.rank` String) — no link to a canonical AAFC rank catalogue, no validation | Implemented commit `16f6bce`: `_AAFC_RANKS` catalogue in training.py; `GET /api/facilitators/ranks` endpoint; `_normalise_rank()` applied on create/update/import; frontend datalist populated from API. 8 regression tests added. | FIXED LOCALLY |
| DEF-05 | Defect | MEDIUM | Legacy "Annual Program" stale text — `connected-frontend/index.html:6842` still says "Annual Program" linking to `nav('planning-year')`; functional redirect exists (`nav('planning-year')→'activities'`) but text misleads users | Confirmed resolved: `nav('planning-year')` as user-visible text no longer exists in index.html; all remaining references are code comments. Gap register was stale (original line number drifted). | CLOSED |
| DEF-06 | Defect | MEDIUM | Wing-onboarding CLI-only — no HTTP API endpoint for Wing provisioning; requires direct server/DB access | FIXED LOCALLY 2026-08-13: `POST /api/system/provision-wing` added to `system.py` — idempotent, system_admin only, creates Wing + Squadrons + initial accounts (wing_admin, sqn_admin, sqn_general) in one HTTP call; returns one-time codes; usable in any environment including production; 10 tests in `test_wing_provision.py` pass (auth, 403, happy path, accounts, no-accounts flag, idempotency, audit log, default-squadron, notice). | FIXED LOCALLY |
| DEF-07 | Defect | MEDIUM | Multi-Wing report cross-scope verification — Wing reports show only their Wing's data; National aggregates all Wings; no cross-Wing data leak | FIXED LOCALLY 2026-08-14: `backend/tests/test_multi_wing.py` — 16 tests using `POST /api/system/provision-wing` to create synthetic 11WG; verifies scope isolation (wing_admin denied national-overview 403; cannot read 11WG squadrons, overview, capability, phase-coverage; cannot bypass via wing_id param); verifies national aggregation (national-overview + national-capability both include 7WG and 11WG); 1681 backend tests pass. Staging verification pending lockout SQL reset + redeploy. | FIXED LOCALLY |
| DEF-08 | Defect | MEDIUM | Celery export task (`generate_export`) is a stub — sync fallback records success without writing a real file; no object-storage or presigned-URL path | FIXED 2026-08-13: `exportProgramItemsCSV()` in connected-frontend switched from job-based path to direct streaming download via `GET /api/export/program-items.csv`. Uses fetch + Blob URL + programmatic `<a>` click pattern; includes Authorization header from `tokenGet()`; handles non-OK responses with actionable toast. Streaming endpoint was already fully implemented in `export_import.py` (generates CSV, logs export, audits action). Job-based path (`POST /api/jobs/export`) remains available for future Celery/Redis integration when exports exceed synchronous timeout. Button tooltip updated to remove "background job" description. | FIXED LOCALLY |
| DEF-09 | Defect | MEDIUM | Background job polling UI — no frontend polling of `GET /api/jobs/{id}` when an async export is in flight; users cannot tell whether a large export is still running | `_jobPollToast(label, jobId)` added to connected-frontend: shows persistent toast with spinner; polls every 2 s; transitions to ok/err on terminal state; `exportProgramItemsCSV()` triggers `POST /api/jobs/export` and wires the toast. "Export CSV" button added to Curriculum page. DEF-08 (real file generation) still blocked on Celery/Redis provisioning. | FIXED LOCALLY |
| DEF-10 | Defect | MEDIUM | DB-backed general rate limiter — in-memory `_api_hits` degrades proportionally with Gunicorn worker count; `GUNICORN_WORKERS ≤ 2` operational cap enforced until Option A (DB-backed) is implemented | Implemented commit `ea811d0`: `IpApiRequest` model + v38 migration; `check_api_rate_db` / `reset_api_rate_limiter_db` in `security.py`; `main.py` uses DB-backed path when `ENVIRONMENT=production/staging`, in-memory in development; `system.py` reset endpoint clears both. 4 regression tests added. `GUNICORN_WORKERS` cap lifted. | FIXED LOCALLY |
| DEF-11 | Defect | MEDIUM | Per-account rate limiting on non-login API endpoints | Implemented commit `111587d`: `UserApiRequest` model + v39 Alembic migration; `check_user_api_rate_db(user_id, db)` called inside `get_principal()` for production/staging after JWT validation; `reset_user_api_rate_limiter_db()` wired into reset_rate_limits endpoint and conftest fixture. 4 regression tests added. | FIXED LOCALLY |
| DEF-12 | Defect | LOW | Maintenance mode frontend banner — users already in session see no in-app banner when write-block is active | Fixed commits `677d5e5`/`dfafc09`: `_maintenancePoll()` at `index.html:4304` polls every 30 s; when block activates, `checkMaintenanceBanner()` renders a banner to authenticated users without requiring page reload. Gap register was stale. | CLOSED |
| DEF-13 | Defect | LOW | Maintenance mode expected return time — no return-time field in `SystemSetting`; cannot be communicated to users | `maintenance_until` key stored via `_set_setting()`; System Console input at `index.html:1640` ("Expected return time"); maintenance banner at `index.html:4736` renders `Expected return: ${d.until}` when set. Gap register was stale. | CLOSED |
| DEF-14 | Defect | LOW | Maintenance mode Celery drain — in-flight jobs may fail silently if write-block activates mid-task | Implemented commit `be8ff14`: `enable_maintenance` queries `JobStatus` for queued/running jobs, marks them cancelled (with reason), commits before the write-block activates. Returns `drained_jobs` count; recorded in audit log. Guards the real-Celery path (stub currently, DEF-08). | FIXED LOCALLY |
| DEF-15 | Defect | MEDIUM | `ENVIRONMENT=staging` in production Railway config (DEFECT-003) — `is_production` and `validate_for_production()` key off this value; risks production safety checks not triggering | Code fix merged; Railway production env var not corrected; requires System Admin action | HUMAN GATE |
| DEF-18 | Defect | HIGH | Training Class audience conflict override never surfaced — `savePNDetail()` and `saveSessEdit()` both caught `class_conflict` 409 from `_saveSessionAudience()` and checked `audienceErr.detail.error` to decide whether to show an inline Override link; but `api()` throws `{kind,status,code,msg,body}` not `{detail}`, so `audienceErr.detail` was always `undefined`, the condition never fired, and the override link was never shown — any class scheduling conflict produced a generic error instead of the "Override" action | Fixed both catch blocks: `audienceErr.detail` → `audienceErr.body?.detail` in `savePNDetail()` (line ~7951) and `saveSessEdit()` (line ~8277); override link now fires correctly when backend returns `class_conflict` with `conflicts[]` array | FIXED LOCALLY |
| DEF-17 | Defect | MEDIUM | Facilitator leave not detected as a conflict — `_run_conflict_check` in `planning.py` checked facilitator double-booking and holiday conflicts but never queried `PlanningFacilitatorLeave`; assigning a facilitator who is on leave generated no conflict warning in either frontend | Added `facilitator_on_leave` to `CONFLICT_TYPES` in `models/planning.py`; added leave detection block to `_run_conflict_check`: queries all `PlanningFacilitatorLeave` records overlapping the parade date for assigned facilitators (one `.in_()` query, deduplicates by `warned_facs` set), creates `warning` severity conflict per affected facilitator with reason and session reference; 1 regression test added `test_facilitator_on_leave_generates_conflict` — PASSED | FIXED LOCALLY |
| DEF-16 | Defect | HIGH | `apiClosePN()` was dead code — backend endpoint `POST /api/parade-nights/{pnid}/close` existed and was fully implemented (checks `close_blockers()`: all sessions in final status; sets `closeout_status='closed'`, records `closed_by`/`closed_at`, writes audit log) but no UI element ever called it. Parade Nights could not be closed from either frontend. | `doClosePN(pnId)` function added using `confirmAction()` pattern; "Close Parade Night" button wired in `showPNDetail()` (shown only when `canWriteSquadron() && pn.closeout_status !== 'closed'`); `close_blocked` error surfaces per-session blockers from `e.body.detail.blockers` via toast. Error handler also maps the generic `close_blocked` code to a readable fallback message (line 3673). | FIXED LOCALLY |
| DEF-19 | Defect | MEDIUM | Wing rename absent — `PATCH /api/wings/{id}` did not exist; `name` and `short_name` could not be changed after creation; only `code` (immutable) was settable at create time | New `WingUpdateIn` Pydantic model + `PATCH /api/wings/{wing_id}` endpoint in `organisations.py`; restricted to `national_admin`/`system_admin`; validates non-empty name (≤120 chars), rejects duplicate active Wing name (409), updates `short_name` (≤40 chars, falls back to code if blank); writes audit log with old/new values. Frontend: shared `m-rename-org` modal + `_openRenameOrgModal()` + `_saveOrgRename()` in `connected-frontend/index.html`; "Rename" button added to Wings table action cell. Tests: `test_nat_admin_can_rename_wing`, `test_wing_rename_requires_nat_admin`, `test_wing_rename_unauthenticated`, `test_wing_rename_empty_name_rejected`, `test_wing_rename_audited` — all passed. Backend: 1689 passed, 6 skipped. | FIXED LOCALLY |
| DEF-20 | Defect | MEDIUM | Squadron name/short_name editing absent — `PATCH /api/squadrons/{id}` accepted `unit_type` and address fields but not `name` or `short_name`; Squadron display names could not be corrected after creation | Extended `SquadronUpdateIn` Pydantic model with `name: str | None` and `short_name: str | None`; added update logic in `update_squadron` (validates non-empty name ≤150 chars, `short_name` ≤40 chars, falls back to code if blank). Frontend: shared `m-rename-org` modal (same as DEF-19) + "Rename" button in Squadrons table action cell. Tests: `test_sqn_admin_can_rename_squadron`, `test_squadron_rename_empty_name_rejected`, `test_squadron_rename_unauthenticated` — all passed. | FIXED LOCALLY |
| DEF-21 | Defect/Privacy | HIGH | Six dead-code JS constants in `connected-frontend/index.html` contained real personnel names — `DEFAULT_FACS` (13 entries with surnames Daley, Figueiredo, McGhie, Mitchell, Milligen, Wallace) and `PN_703` (65 lines of parade history with surnames Mitchell, Milligen, Daniels, Brunet de Rochebrune, Flanders, Sundaram, Wallace, McGhie). Also orphaned: `CORE_CURR` (41-line national curriculum array superseded by API), `DEFAULT_ACTS`, `DEFAULT_ROOMS`, `DEFAULT_EQUIP`. None referenced anywhere (each appeared exactly once at declaration, confirmed via `grep -c`). Privacy risk: real identifiable names in a shipped production frontend file. | Deleted entire block (lines 3408–3562, 155 lines) via `sed -i '' '3408,3562d'`. `SQN_DATA` (lines 3385–3403) retained — it is actively referenced by `getCurrentUnitInfo()` as a fallback for display names. | FIXED LOCALLY |
| DEF-22 | Defect/Correctness | MEDIUM | `getCurrentUnitInfo()` early-returned `SQN_DATA[S.sqn]` unconditionally for any of the 17 7WG units (`if(meta)return meta`), bypassing `S.cfg` entirely. Effect: backend-configured parade time changes made in Unit Settings (default parade day, start time, end time, session count, address) were silently ignored in the weekly-program print header, the schedule preview, and any other call site using this function for 7WG units. Secondary bug: the fallback branch returned `addr:''` always, even when `S.cfg.addr` was non-empty. | Rewrote `getCurrentUnitInfo()` to always prefer `S.cfg` backend values for operational fields (`day`, `start`, `end`, `sess`, `addr`), using `SQN_DATA` as fallback only when `S.cfg` has no value. `name` and `short` continue to use `SQN_DATA` as primary (display strings not backend-configured). `addr` now correctly propagates from `S.cfg` or `SQN_DATA` rather than always returning `''` for the non-SQN_DATA fallback path. | FIXED LOCALLY |
| DEF-23 | Defect/UX | LOW | Six save handlers in `connected-frontend/index.html` had no success feedback — modal closed silently on success while showing errors on failure. Affected: `saveSessEdit()` (quick-edit session), `doSaveGettingHelp()` (Getting Help content), `saveRoom()` (add/edit room), `saveEquip()` (add/edit equipment), `doSaveSession()` (Planning Workspace builder session modal), `saveWingEvent()` (Wing HQ calendar event). Users had no confirmation that the save succeeded vs closed via a background error — particularly confusing for `saveSessEdit` (high-frequency operation). By contrast, `saveSettings()`, `saveTimingTemplate()`, and `saveFac()` all showed explicit confirmation. | Added `showToast()` call on success path for all six handlers. `saveRoom` and `saveEquip` use distinct "added" vs "updated" messages based on whether `editRoomId`/`editEquipId` was set before the save (captured in `isEditRoom`/`isEditEquip` before `editRoomId=null` clears it). `saveWingEvent` uses "Event created." vs "Event updated." via `isEditEvent`. | FIXED LOCALLY |
| DEF-24 | Defect/Reliability | LOW | `navigator.clipboard.writeText()` in `frontend/src/routes/Accounts.tsx` (line 43, `NewCodeModal.copy()`) had no `.catch()`. `clipboard.writeText` rejects when the document is not focused, the page is not served over HTTPS, or the user denies clipboard permission — in those cases the rejection was unhandled (unhandledRejection event in Chrome, console warning). The `setCopied` state update was silently skipped with no user feedback. | Added `.catch(()=>{})` to the `.then()` chain to silently suppress the rejection — the visual "Copied" indicator simply does not appear if the clipboard operation fails, which is correct behaviour. | FIXED LOCALLY |
| DEF-25 | Test Coverage | LOW | `frontend/e2e/facilitators.spec.ts` had no UI tests for the Edit or Archive mutation flows. Only the "add" and "read" paths were exercised. The Edit modal (`role="dialog" name="Edit facilitator"`, fields `#ef-first`, `#ef-last`, `#ef-rank`, `#ef-type`, Save button) and Archive confirm flow (`role="alertdialog"` via `useConfirm()` hook, Archive confirm button) were untested. The "add" test also used a hardcoded last name ("TestFac") that triggered the 409 `possible_duplicate` flow on repeat runs, making the test non-idempotent. | Added section 3 (Edit test) and section 4 (Archive test) to `facilitators.spec.ts`. Both use `Date.now()` suffix for isolation. Edit test: adds unique facilitator, clicks Edit, fills `#ef-first`="UpdatedFirst" and `#ef-rank`="FLTLT", saves, asserts updated full name `UpdatedFirst EditTest${suffix}` is visible. Archive test: adds unique facilitator, clicks Archive, confirms `role="alertdialog"`, asserts facilitator no longer visible. Fixed "add" test to use `AddTest${suffix}` instead of hardcoded "TestFac". Fixed edit assertion to use full name string (avoids Playwright strict-mode error when multiple "UpdatedFirst" records accumulate from repeated runs). Commit `05435b6`. | FIXED LOCALLY |
| DEF-26 | Defect/UX | LOW | Two residual save-feedback gaps found by autosave audit (2026-08-15): (1) `saveTagFac()` in `connected-frontend/index.html` (line 9555) called `closeModal('m-tag-fac')` immediately on success without any toast — the modal closed silently with no confirmation the tags were saved. This contrasts with all other save handlers that call `showToast()`. (2) Nine `useMutation` calls in `frontend/src/routes/Accounts.tsx` had no `onError` handler — `disableMut`, `reactivateMut`, `editMut`, `archiveMut`, `restoreMut`, `unlockMut`, `createFlightMut`, `renameFlightMut`, `archiveFlightMut`. On API failure the button re-enabled silently with no user-visible error; the table did not update because `invalidateQueries` is only in `onSuccess`. | (1) Added `showToast('Subject areas saved.')` before `closeModal('m-tag-fac')` in `saveTagFac()`. (2) Added `onError: (e: Error) => toast(friendlyMessage(e, "Could not ..."), true)` to all nine mutations, with action-specific messages ("Could not disable account.", "Could not archive flight.", etc.). Added `import { useToast } from "../components/Toast"` and `const { toast } = useToast()` to `Accounts.tsx`. TypeScript: 0 errors. | FIXED LOCALLY |
| DEF-27 | Defect/UX | LOW | `_populateCreateWings()` in the Create Account modal pre-selects and locks the wing dropdown for wing_admin (`S.isWing`) but not for sqn_admin — the dropdown rendered with the correct option loaded but nothing selected, causing `doCreateAccount()` to block submission with "Select a Wing." even though the wing is unambiguously the sqn_admin's own wing (identical to the squadron's wing). The backend derives `wing_id` from the squadron record when absent (accounts.py lines 319–323), so this was a frontend-only validation gap. | Extended `_populateCreateWings()` auto-select condition from `S.isWing` to `S.isWing \|\| S.role==='sqn_admin'`, using `S.session.wing_id`. Wing dropdown is now pre-selected and disabled for sqn_admin on modal open. One-line change in `connected-frontend/index.html`. | FIXED LOCALLY |

---

## 8. Visual Design & Accessibility

**Scope:** Axe staging tests (34 tests across `tools/playwright-staging/`) verify WCAG rules but are
not in main CI. Level A does not require formal WCAG certification. Accessibility automation in CI
and a formal conformance declaration are Level B/C requirements. Shared design tokens are Level B.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| VIS-01 | Visual | MEDIUM | Shared CSS design tokens — Main TMS (`--blue`, `--dark`) and Planning Workspace (`--aafc-blue`, `--aafc-dark-blue`) use divergent token names on the same AAFC VIG palette | `shared/tokens.css` created as canonical source (commit `c255bc4`). Planning Workspace `tokens.css` now `@import '../../../shared/tokens.css'` + adds backward-compat `--aafc-*` aliases so 146 existing component call sites work unchanged. Connected-frontend tokens kept inline (nginx serves a single HTML file) with sync markers. Vite build verified: canonical `--blue` and backward-compat `--aafc-blue: var(--blue)` both present in built CSS. | FIXED LOCALLY |
| VIS-02 | Accessibility | HIGH | Axe automation in main CI — accessibility checks run on every push, not only in staging-specific scripts | `frontend/e2e/accessibility.spec.ts` (comprehensive Axe WCAG 2.1 AA, 20 pages/roles) is in `testDir=./e2e/` and runs with `npx playwright test`. `.github/workflows/e2e-tests.yml` added (2026-08-13): runs both Planning Workspace (21 specs) and Connected Frontend (30 specs) E2E suites on push/PR to main/release/next-stage. | FIXED LOCALLY |
| VIS-03 | Accessibility | MEDIUM | High-contrast mode — `forced-colors` / `prefers-contrast: more` media queries honoured throughout both frontends | Implemented commit `0a56fa0`: connected-frontend has `@media (prefers-contrast: more)` (token strengthening, bolder borders/focus) + `@media (forced-colors: active)` (Highlight system colour, forced borders); Planning Workspace tokens.css improved `data-theme="hc"` + same two media queries added. | FIXED LOCALLY |
| VIS-04 | Accessibility | MEDIUM | Keyboard-only workflow coverage — all primary workflows completable without a pointer device | FIXED 2026-08-13: connected-frontend nav items (`div.nav-item`) given `tabindex="0"` + delegated `keydown` handler (Enter/Space → `.click()`) + `applyNavScope()` tabindex management (shown=`0`, hidden=`-1`) + `:focus-visible` CSS. 1584 backend tests pass. Login (button elements), modals, tooltips already keyboard-accessible. E2E tests added 2026-08-13: `accessibility-hardening.spec.ts` — VIS-04 tests verify (1) all visible nav items have `tabindex="0"` after login, (2) Enter key on Parade Nights nav item activates that page, (3) Space key on Dashboard nav item activates that page. | FIXED LOCALLY |
| VIS-05 | Accessibility | MEDIUM | SC 1.4.3 Color Contrast (WCAG AA) — all text/background pairs meet 4.5:1 minimum (3:1 for large text) | `a11y-wcag.spec.ts` tests with Axe `color-contrast` rule in staging for all four roles; not in main CI | STAGING VERIFIED |
| VIS-06 | Accessibility | MEDIUM | SC 1.4.10 Reflow at 320 px — content usable without horizontal scrolling at 320 px viewport | `a11y-wcag.spec.ts` tests this in staging; not in main CI | STAGING VERIFIED |
| VIS-07 | Accessibility | MEDIUM | SC 1.4.4 Resize Text — all content functional at 200% zoom (640 px simulation) | `a11y-wcag.spec.ts` tests this in staging; not in main CI | STAGING VERIFIED |
| VIS-08 | Accessibility | LOW | No-duplicate-IDs and aria-label rules verified for all four roles (system_admin, sqn_admin, wing_admin, national_admin) | `a11y-staging.spec.ts` covers all four roles; not in main CI | STAGING VERIFIED |
| VIS-09 | Accessibility | LOW | WCAG 2.1 AA formal conformance declaration for National rollout | Declaration written at `docs/next-stage/vis-09_wcag_conformance_declaration.md`. EXTENDED 2026-08-13: (1) `nav()` now sets `aria-hidden="true"` on all inactive `.page` divs and removes it from the active page — resolves heading-structure (multiple H1s visible to AT) and landmark-region (AT sees all pages' content simultaneously) complaints. (2) Remaining 29 selects without `aria-label` audited: 26 have correct `<label for>` associations or implicit label wrapping (fully accessible); 1 (`ca-flight`) is in `aria-hidden` parent div when hidden; 1 timing-block-type select in JS template given `aria-label="Block type"` (L10258); 1 uses implicit wrapping label (L1388). Connected-frontend gaps now reduced from 3 to 0 structural issues; Planning Workspace conformance unchanged (passes). EXTENDED 2026-08-14: 8 `×` action buttons missing `aria-label` identified and fixed — Delete Parade Night (L7530), Delete Curriculum Item (L8662), Delete Activity (L9351), Archive Facilitator (L9621), Archive Training Area (L10085), Archive Equipment (L10095), Cancel New Phase (L2527), Cancel New Element (L2542); each given a descriptive `aria-label` matching its action. | FIXED LOCALLY |
| VIS-10 | Visual | LOW | Multi-Wing E2E playwright tests — visual and data-scope verification with a second Wing activated in staging | FIXED LOCALLY 2026-08-14: `frontend/e2e/multi-wing-scope.spec.ts` — 14 tests; provisions synthetic 11WG in `beforeAll` via API; verifies (1) national admin sees both 7WG + 11WG in `/national-overview` UI and API; (2) wing_admin (7WG) sees only 7WG squadrons in `/wing-overview`; (3) 11WG squadron (1101) absent from 7WG wing-overview; (4) wing_id param bypass blocked; (5) 403 on national API endpoints for wing_admin; (6) 401 for unauthenticated. TypeScript: 0 errors. Full staging run pending lockout SQL reset + redeploy. | FIXED LOCALLY |
| VIS-11 | Visual | MEDIUM | Phase 7 typography / readability pass — connected-frontend micro-label sizes (9–10px), form-label sizes (10px), body font (13px), button targets, and form field spacing below comfortable thresholds for non-technical users | EXTENDED 2026-08-13: **Round 1** (connected-frontend): body `13px→14px`, `line-height 1.55→1.6`; `.fg label` and `.form-group label,.ff label` `10px→12px`, `margin-bottom 4–5px→6px`; `.btn` `7px 14px / 11.5px→9px 16px / 13px`; `.btn-sm` `5px 10px / 10.5px→6px 12px / 11.5px`; `.btn-xs` `3px 8px / 10px→4px 9px / 11px`; `.nav-lbl` `9px→10.5px`; `.slbl` `9.5px→11px`; `.ctitle` `10px→11.5px`; `.tab-btn` `10px→11.5px`; `.form-group,.ff margin-bottom 13px→18px`; `.form-row gap 12px→16px`; `.modal-foot padding 12px→16px`. **Round 2** (connected-frontend + Planning Workspace): `.ssub` `10px→11px`; `.badge` `9.5px→10.5px`; `.nav-planning-hdr` `9px→10.5px`; `.dash-sec-hdr` `10px→11px`; `.ph-cnt` `10.5px→11.5px`; `.chart-data-toggle` `10.5px→11.5px`; `.wp-tbl th` (Weekly Program table headers, critical for print) `9px→10.5px`; `.clash-badge` `9.5px→11px`; `.fac-sl` `9px→10.5px`; "NEEDS ATTENTION" label `9.5px→11px`; print weekly-program header ("7 Wing AAFC") `9px→11px`; "NAT HQ"/"Wing" activity source badges `8px→10px`; parade-night facilitated-sessions meta "No sessions scheduled"/"N sessions" `9.5px→11px`; parade-night detail section headers ("Session N", "Notices", "Parade Night Details") `9.5px→11px`. Planning Workspace `planning.css`: 8 safe meta-label bumps (`pw-section-hdr`, `pw-backlog-code`, `pw-backlog-card-code`, `pw-backlog-card-meta`, `pw-fac-table th`, `pw-lv-p-num`, `pw-lv-p-detail`, `pw-lv-empty-period` all `10px→11px`). Compact block/grid overrides (`pw-block-sm`, `pw-block-cg-*`, 9.5px) left — load-bearing for tight calendar cell layouts. TypeScript: 0 errors. Backend: 1618 passed, 5 skipped (unaffected). | FIXED LOCALLY |

---

## 9. Security & Hardening

**Scope:** OWASP Top 10 audit (`15_security_hardening_review.md`) rated A01–A05, A07–A10 as complete
or improved. Remaining gaps are A06 (dependency scanning not in CI), A09 (external alerting), and
production-deployment verification steps that require a live HTTP check, not code inspection alone.
Four gaps require System Admin action as Human Gates.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| SEC-01 | Security | HIGH | CSRF env var verification — System Admin confirms `COOKIE_SAMESITE=none` and `COOKIE_SECURE=true` in Railway production dashboard; confirms `CORS_ALLOWED_ORIGINS` has no wildcard | **2026-08-16 live curl check:** Staging CORS verified — `access-control-allow-origin: https://aafc-tms-frontend-staging.up.railway.app` (no wildcard); `Set-Cookie: aafc_session=...; HttpOnly; Max-Age=1800; Path=/; SameSite=none; Secure` ✓. Production CORS preflight verified — `access-control-allow-origin: https://aafc-tms-frontend-production.up.railway.app` (no wildcard), `access-control-allow-credentials: true` ✓. **Production `Set-Cookie` header not yet curl-checked** (requires login call — awaiting SA manual confirmation via Railway dashboard that `COOKIE_SAMESITE=none` and `COOKIE_SECURE=true` are set). Engineering half complete — human confirmation outstanding. | HUMAN GATE |
| SEC-02 | Security | HIGH | Backup key custody — GPG private key stored offline, GitHub Secrets updated, daily backup manually confirmed, first restore test result reviewed | Public key committed to repo; `v1_go_no_go_checklist.md §C` documents steps; not yet completed by System Admin | HUMAN GATE |
| SEC-03 | Security | HIGH | DR rehearsal — disaster recovery rehearsal run per `19_disaster_recovery_rehearsal.md`; result recorded in Evidence Table | Procedure written; never run | HUMAN GATE |
| SEC-04 | Security | CRITICAL | External penetration test — grey-box, 5-day engagement per `22_pen_test_scope.md`; Critical and High findings remediated before National rollout | Scope documented; vendor not engaged; budget and organisational approval required | HUMAN GATE |
| SEC-05 | Security | MEDIUM | Alerting on failed-login spike — active notification when login failure rate exceeds threshold in a rolling window | `record_login_failure_db()` in `security.py` now emits `{"event":"security_alert","type":"login_spike",...}` WARNING to the `security` logger at `LOGIN_MAX_ATTEMPTS` and every 5 additional failures thereafter. Tests in `test_rate_limiting.py` (`test_login_spike_emits_security_log`, `test_login_spike_repeats_on_subsequent_multiples`) pass. Structured log event is the alert mechanism; external channel (Slack/PagerDuty) remains a deployment integration step. | FIXED LOCALLY |
| SEC-06 | Security | MEDIUM | Alerting on 5xx error rate above threshold | `access_log` middleware in `main.py` tracks a per-process rolling 60s deque of 5xx responses; when count ≥ `_5XX_ALERT_THRESHOLD` (5), emits `{"event":"security_alert","type":"5xx_spike",...}` WARNING to `security` logger. Test `test_5xx_spike_emits_security_log` passes. External alert channel integration remains a deployment step. | FIXED LOCALLY |
| SEC-07 | Security | LOW | Alerting on daily backup workflow failure — active channel beyond GitHub Actions default email | FIXED 2026-08-13: `notify-on-failure` job added to `backup-postgresql.yml` — posts Slack alert if `SLACK_BACKUP_ALERT_WEBHOOK` secret is configured; silently skips if not (GitHub default email still fires). Context values passed via `env:` block, not inline interpolation. System Admin must add the webhook secret to activate. | FIXED LOCALLY |
| SEC-08 | Security | MEDIUM | Dependency vulnerability scanning in CI — `pip-audit` (backend) and `npm audit --audit-level=high` (React frontend) in an automated workflow before each release | `.github/workflows/dependency-audit.yml` added: runs on push/PR to main/release branches and weekly Monday 09:00 UTC; `pip-audit --strict` for backend, `npm audit --audit-level=high` for Planning Workspace | FIXED LOCALLY |
| SEC-09 | Security | MEDIUM | CSP `connect-src` runtime injection verified in deployed connected-frontend nginx response | Live HTTP check 2026-08-13: staging response includes `connect-src 'self' https://aafc-tms-backend-staging.up.railway.app`; production response includes `connect-src 'self' https://aafc-tms-backend-production.up.railway.app` — per-environment injection confirmed working. | STAGING VERIFIED |
| SEC-10 | Security | LOW | `Permissions-Policy` header confirmed in production connected-frontend nginx HTTP response | Live HTTP check 2026-08-13: production frontend returns `permissions-policy: geolocation=(), microphone=(), camera=()`. Staging also confirmed. | STAGING VERIFIED |
| SEC-11 | Security | MEDIUM | Token version revocation triggered automatically on account disable or role change — not only on code reset | `disable_account()` at `accounts.py:628` now increments `token_version` before committing, immediately invalidating live JWTs; role change and scope change already incremented it. Regression test `test_disable_invalidates_existing_jwt` confirms 401 on reuse | FIXED LOCALLY |
| SEC-12 | Security | LOW | Quarterly DR rehearsal schedule established — first rehearsal date set and rehearsal completed | Schedule written at `docs/next-stage/27_dr_rehearsal_schedule.md`: Q3 2026 rehearsal due 2026-08-27 (System Admin); quarterly cadence Jan/Apr/Jul/Oct thereafter. First rehearsal result (DOC-06) still pending. | FIXED LOCALLY |
| SEC-13 | Security | LOW | HSTS header confirmed in production backend HTTP response — injected by `security_headers` middleware when `is_production=True` | Live HTTP check 2026-08-13: production backend returns `strict-transport-security: max-age=63072000; includeSubDomains` on `/api/health/ready`. | STAGING VERIFIED |
| SEC-14 | Security | LOW | `/docs`, `/redoc`, `/openapi.json` confirmed absent from production via live HTTP request | Live HTTP check 2026-08-13: `GET /docs` on production backend returns HTTP/2 404. Gate confirmed working. | STAGING VERIFIED |
| HARD-01 | Hardening | MEDIUM | Stale data on tab focus — when a user returns to a backgrounded tab after ≥5 minutes, operational data (parade nights, sessions, reports) may reflect another user's stale changes made while this tab was hidden | FIXED 2026-08-13: `S._loadedAt` timestamp set on every successful `loadData()` call. `visibilitychange` handler extended: when tab returns to foreground and `loadedAt` is absent or ≥5 min old, calls `loadData()` + `renderAll()` silently, then shows a non-blocking "Data refreshed." toast. On failure, error is swallowed (no blocking `alert()`). Planning Workspace already has `refetchOnWindowFocus: true` at QueryClient level (`App.tsx:36`). | FIXED LOCALLY |
| HARD-02 | Hardening | LOW | Blocking alert() dialogs on failure — several write-operation error paths in connected-frontend used native `alert()` which freezes the JavaScript event loop and blocks all background polling (maintenance status, visibility refresh) while open | FIXED 2026-08-13 (extended sweep): all non-system-console `alert()` calls now converted to `showToast()` — covering `addPN()`, planning notices, curriculum edit/delete, activity archive/create/delete/clear-override, facilitator delete, room save/delete, equipment save/delete, settings save, parade-day propagation, proxy/delegated intervention enter/exit, parade date delete, generate-dates success, holiday delete, Training Class open/archive/split/merge, anchor event open/delete, run-checks success/error, conflict override, exports (annual program, schedule, download), Wing HQ event edit/archive, Wing event mark-reviewed/mark-planned. Only `alert()` calls remaining are in System Console (explicitly documented as deliberate) and `gate()` (developer stub for unimplemented features). | FIXED LOCALLY |
| HARD-03 | Hardening | LOW | `prefers-reduced-motion` — users who have enabled OS-level Reduce Motion receive full CSS animations (shimmer skeleton, spinner, button/input/modal transitions) which can cause discomfort or trigger vestibular disorders | FIXED 2026-08-13: `@media(prefers-reduced-motion:reduce)` block added immediately before `</style>` in `connected-frontend/index.html`. Uses universal selector (`*,*::before,*::after`) with `animation-duration:.01ms!important`, `animation-iteration-count:1!important`, `transition-duration:.01ms!important`, `scroll-behavior:auto!important` — covers `@keyframes shimmer` (skeleton loading), `@keyframes _spin` (job spinner), and all transition-* properties on buttons, inputs, modals, and nav items without needing to enumerate them individually. Planning Workspace inherits Vite/React ecosystem `prefers-reduced-motion` awareness via standard CSS media queries in component styles. | FIXED LOCALLY |
| HARD-04 | Hardening | MEDIUM | Keyboard focus visibility — `.btn`, `.btn-auth`, `.tab-btn`, `.seg-btn`, and `<a>` elements had no `:focus-visible` style in the standard (non-high-contrast) theme; keyboard users had no visible cue of their current interactive position | FIXED 2026-08-13: Added `.btn:focus-visible,.btn-auth:focus-visible{outline:2px solid var(--blue);outline-offset:2px;}`, `a:focus-visible{outline:2px solid var(--blue);outline-offset:2px;border-radius:2px;}`, `.tab-btn:focus-visible{outline:2px solid var(--blue);outline-offset:-2px;}`, `.seg-btn:focus-visible{outline:2px solid var(--blue);outline-offset:-2px;z-index:1;position:relative;}`. Also added `box-shadow:0 0 0 3px rgba(81,176,227,.15)` to `.fbar select:focus,.fbar input:focus` to match the treatment already applied to form-group and auth inputs. High-contrast-mode and forced-colors `:focus-visible` rules already present (lines 117–119, 149) are unchanged. Skip-to-main-content link (`skip-link`) already implemented. | FIXED LOCALLY |
| HARD-06 | Hardening | LOW | 200% zoom / narrow viewport — `.sc-audit-row` (fixed `140px 70px 120px 1fr` columns) and `.sc-kv` (fixed `180px 1fr` columns) overflow horizontally below 600px viewport width, violating WCAG 1.4.10 Reflow at high zoom levels | FIXED 2026-08-13: Added `@media(max-width:600px)` block: `.sc-audit-row` switches to `1fr 1fr` (2-column); `.sc-kv` switches to `1fr` (stacked label+value). Mobile layout already handled by existing 768px breakpoint (hamburger nav, form-row 1fr, stats-grid 2-col). Dashboard grids already have responsive breakpoints at 860px and 1000px. | FIXED LOCALLY |
| HARD-07 | Hardening | MEDIUM | Accessibility E2E smoke tests — no automated tests verified ARIA landmarks, modal focus management, skip link, toast aria-live, hamburger aria-expanded, prefers-reduced-motion CSS rule, or focus-visible CSS rule | FIXED 2026-08-13: `e2e-connected/accessibility-hardening.spec.ts` added (8 tests): (1) `<nav>` and `<main>` landmarks present; (2) skip-link in DOM targeting `#main-content`; (3) toast region `aria-live="polite"`; (4) opening `#m-add-pn` modal moves focus inside it; (5) Escape closes the modal; (6) `.btn:focus-visible` outline rule present in stylesheet; (7) `prefers-reduced-motion` media rule present in stylesheet; (8) hamburger `aria-expanded` toggles at mobile viewport width (390px). Additional E2E coverage added same session: `parade-night-bulk-ops.spec.ts` (6 tests: WORK-11 copy modal + API, WORK-12 cancel-all button + API, DEF-08 export CSV auth header + streaming endpoint); DASH-10 adoption analytics tests added to `training-dashboard.spec.ts`. Total e2e-connected: 33 spec files. | FIXED LOCALLY |
| HARD-05 | Hardening | MEDIUM | Screen reader basics — modal dialogs lacked focus management: opening a modal left focus behind the backdrop, making keyboard and screen reader traversal non-functional inside the dialog; Escape key did not close modals | FIXED 2026-08-13: `openModal(id)` now saves `document.activeElement` to `_modalReturnFocus`, then sets focus to first focusable element inside the modal after 40ms (avoids layout-not-ready race). `closeModal(id)` restores focus to `_modalReturnFocus`. Global Escape handler extended to close the topmost `.modal-bg.active` element (excluding `#m-outcome-reason` which has its own handler) by triggering `.modal-x` click if present, or falling back to direct class removal + focus restore. Hamburger `aria-expanded` was already dynamically updated in `toggleMobileNav()`/`closeMobileNav()`. Toast region already had `aria-live="polite" aria-atomic="true"`. `<nav>` and `<main>` semantic landmarks already present. | FIXED LOCALLY |
| HARD-09 | Hardening | MEDIUM | Blocking `prompt()` dialogs — 9 native `prompt()` calls in connected-frontend collected text synchronously, freezing the JS event loop, breaking browser automation, and inaccessible to screen readers | FIXED 2026-08-13: `m-text-input` modal + `promptText(title, label, {context, defaultValue, okLabel, validate, danger})` Promise-based helper added. Enter key saves; Escape cancels via global handler. `validate(val)→errorString` param supports type-to-confirm deletion patterns. Converted: `_onReasonChange()` (new outcome reason tag), `_onFacTypeChange()` (new facilitator type tag), `doPermanentlyDeleteWing()`, `doPermanentlyDeleteSquadron()`, `doPermanentlyDeleteAccount()`, `doRenamePlanningYear()`, `doDeletePlanningYear()`, `enterMode()` (proxy/intervention reason — audited), `promptOverrideConflict()`. No native `prompt()` calls remain in user-facing flows. 4 E2E tests added to `accessibility-hardening.spec.ts` (ARIA attrs, open/submit, Escape-cancel, validate-blocks-submit). | FIXED LOCALLY |
| HARD-08 | Hardening | MEDIUM | Blocking `confirm()` dialogs on destructive actions — 17 destructive-action handlers in connected-frontend used native `confirm()` which blocks the JavaScript event loop, breaks browser automation (Playwright hangs), and is inaccessible to screen readers | FIXED 2026-08-13 (connected-frontend: all 21 native confirm() calls converted): `_archiveRefItem()`, `_restoreRefItem()`, `doArchiveFlight()`, `delPN()`, `deleteCurrItem()`, `_actArchive()`, `_actRestore()`, `delAct()`, `clearActivityLocalOverride()`, `delFac()`, `delRoom()`, `delEquip()`, `_closeActionItem()`, `doArchivePlanningYear()`, archive-fallback in `doDeletePlanningYear()`, `deleteParadeDate()`, `deleteHoliday()`, `archiveTrainingClass()`, `deleteAnchor()`, `archiveWingEvent()`, post-squadron-create account prompt. EXTENDED 2026-08-13 (Planning Workspace React app): `ConfirmProvider`/`useConfirm()` context created (`ConfirmDialog.tsx`), added to `App.tsx` provider tree. Converted: `Facilitators.tsx` (archive facilitator), `Accounts.tsx` (disable + archive account, archive flight, archive ref item), `PlanningRightDrawer.tsx` (`handleDelete` session), `PlanningBottomDrawer.tsx` (`BacklogContent` reschedule-confirm, `FacilitatorLeaveSection` `handleDeleteLeave`, `HolidaysContent` `handleDelete`). No native user-facing `window.confirm()` calls remain in either frontend; only System Console `alert()` calls (deliberate) and `gate()` developer stub remain. `datetime.utcnow()` deprecation fixed in `test_rate_limiting.py`. Pre-existing TypeScript error in `permissions.test.tsx` fixed (ProxySession fixture objects missing `acting_squadron_code`/`acting_squadron_name` fields). TypeScript compiles clean (0 errors). | FIXED LOCALLY |
| HARD-10 | Hardening | LOW | Cross-browser E2E test coverage — both `playwright.config.ts` (Planning Workspace) and `playwright.connected.config.ts` (Main TMS) now include `projects` for Chromium, Firefox, and WebKit. 10/10 auth.spec.ts tests pass on all three browsers (30/30 total, 2026-08-15). Earlier "Firefox failures" were caused by a backend connection-pool split state (SQLAlchemy held open file descriptors to two different inode versions of `aafc_tms.db` after a seed operation while the server was live) — not a browser-specific defect. Root cause documented: running `manage.py --seed` while the backend is live can create orphaned inode references in the connection pool; always restart the backend after a reseed. No Firefox/WebKit-specific auth or cookie defect found. Cross-origin session handoff proven: `e2e-connected/cross-origin-session-handoff.spec.ts` (3 tests, commit `b209d05`) — login via TMS (:8080), navigate to PW (:5173), auto-authenticate via `aafc_session` cookie (SameSite=Lax on localhost; cookies sent same-site cross-port). Logout from TMS invalidates PW session. CI extended: both jobs now matrix across chromium/firefox/webkit (6 parallel jobs total; commit `b209d05`). | FIXED LOCALLY |

---

## 10. Documentation & Release Assurance

**Scope:** V1 deployed 2026-08-12. Post-release actions H2–H5 are due within 7 days of deployment.
Level B and C documentation items (Tier 3 reports, Wing onboarding API, capability manifest refresh)
are not yet started. The support runbook and year rollover procedure are CLOSED at Level A.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| DOC-01 | Docs | HIGH | Support runbook — 280-line operator runbook covers common failures, Railway deploy, account recovery, DR steps, incident response; written for non-developer operators | `25_support_runbook.md` complete and reviewed; ownership table not yet filled (H2) | CLOSED |
| DOC-02 | Docs | HIGH | Year rollover E2E procedure proven in staging — 10/10 tests pass; annual operator procedure documented | `test_year_rollover_e2e.py` 10/10 pass; `08_year_rollover_procedure.md` complete; not yet exercised in production | STAGING VERIFIED |
| DOC-03 | Docs | MEDIUM | Playwright E2E — multi-Wing scope tests pass with a second Wing activated in staging | FIXED LOCALLY 2026-08-14: see VIS-10 — same `multi-wing-scope.spec.ts` satisfies both. Staging run pending. | FIXED LOCALLY |
| DOC-04 | Docs | HIGH | Named support ownership — `25_support_runbook.md` Part 1 ownership table completed with named contacts | Support runbook content complete; ownership table empty; post-release action H2 | HUMAN GATE |
| DOC-05 | Docs | HIGH | Weekly restore test — first-week `test-restore-postgresql.yml` GitHub Actions pass confirmed after production deploy | 2026-08-09 run FAILED: `Migration HEAD mismatch: got 'f6a7b8c9d0e1', expected 'ed6feec8b9cd'` — the 2026-08-09 production backup was taken before the 2026-08-12 production deployment (which ran `alembic upgrade head` to bring DB forward). This is an expected pre/post-deployment ordering, not a data loss event. Next scheduled run (2026-08-16) should confirm the post-deployment state. REQUIRED_TABLES list updated 2026-08-13 to include training_classes, session_audiences, cadet_class_memberships, ip_api_requests, user_api_requests, facilitators, parade_nights, ip_login_attempts. | IMPLEMENTING |
| DOC-06 | Docs | MEDIUM | First quarterly DR rehearsal run and result recorded in Evidence Table | Not yet run; post-release action H4 | NOT STARTED |
| DOC-07 | Docs | MEDIUM | V1 go-live communicated to 7WG beta testers | Not yet done; post-release action H5 | NOT STARTED |
| DOC-08 | Docs | HIGH | Beta feedback register populated — all 7WG beta-tester findings classified; critical/high items resolved | `02_beta_feedback_register.md` template exists; items not yet populated (requires beta testers) | HUMAN GATE |
| DOC-09 | Docs | LOW | Defence Writing Manual Chapters 4/6/14/23/24 paragraph-level citation pass applied to all TMS UI copy | FIXED 2026-08-13: see WRITE-11 — same work; active-voice pass applied to confirmation dialogs and empty-state messages; all §§2.53–2.58 / §2.13 / §2.14 rules applied across connected-frontend | FIXED LOCALLY |
| DOC-10 | Docs | HIGH | Wing onboarding runbook §0 governance gate — organisational approval obtained before activating any Wing beyond 7WG in production | `10_wing_onboarding_runbook.md` complete; governance gate not yet passed for any second Wing | HUMAN GATE |
| DOC-11 | Docs | MEDIUM | Report catalogue Tier 3 — National-level reports defined, implemented, and verified with multi-Wing data | Tier 1 (5 sqn) implemented; Tier 2 (5 wing) wired; Tier 3 backend endpoints exist (`/api/reports/national-overview`, `/api/reports/national-capability`) and verified locally with multi-Wing synthetic data via `test_multi_wing.py` (16 tests, all pass). Staging verification pending lockout SQL reset + redeploy. Three ReportCatalogue.tsx items (`national-version-adoption`, `annual-assurance-pack`, `promotion-pipeline`) remain `backend:"no"` — deferred `priority:"later"`. | FIXED LOCALLY |
| DOC-12 | Docs | MEDIUM | Wing onboarding HTTP API — endpoint equivalent of `second_wing_seed.py` for provisioning a Wing without direct server/DB access | FIXED LOCALLY 2026-08-13: see DEF-06 — `POST /api/system/provision-wing` implements the HTTP equivalent. Wing Onboarding Runbook (`10_wing_onboarding_runbook.md`) updated 2026-08-13 (commit `edaee16`) to document `provision-wing` as the primary recommended path, with §2.1–2.3 retained as manual alternatives; §5 (Production Activation) updated to use the endpoint; §7.4 and §8 updated for idempotency behaviour. | FIXED LOCALLY |
| DOC-13 | Docs | LOW | Capability manifest regenerated after Training Class model additions (v48–v50) — diff confirms no route or table removed | Regenerated 2026-08-13: 299 routes / 63 tables (was 273); new routes include `/api/facilitators/ranks`, `/api/planning/anchors/{id}/restore`, and 24 others added since last generation; no routes removed | CLOSED |
| DOC-14 | Docs | MEDIUM | CEA import / CEA relationship documentation — `export_import.py` handles CEA imports; how CEA activities relate to curriculum items and Training Classes under the new model is not yet documented for operators | `docs/next-stage/14_cea_import_operator_guide.md` written 2026-08-13: covers what CEA is, export/import steps, classification workflow, CeaActivity→Session→SessionAudience→TrainingClass path, Squadron views, repeated imports, troubleshooting table, and role summary. | FIXED LOCALLY |
| DOC-15 | Docs | LOW | Role matrix updated to include Training Class operations — who can create, archive, split, and merge a TrainingClass; whether wing_admin can act cross-squadron | `docs/role_matrix.md` §V17 added 2026-08-13: full Training Class operation table (view, create, rename, archive, restore, split, merge, curriculum progress, session audience) with cross-squadron split/merge restriction and history-preservation notes documented. | FIXED LOCALLY |
| DOC-16 | Docs | LOW | `ReportCatalogue.tsx` stale entry — "National capability" was marked `frontend: "no"` but is fully implemented via `Overviews.tsx` `NationalOverview()` Capability tab + `/api/reports/national-capability`; stale data caused the implemented count to be understated by 1 and the "must-have" priority row to appear as unbuilt | FIXED LOCALLY 2026-08-13: `frontend/src/routes/ReportCatalogue.tsx` line 19 updated from `frontend: "no"` to `frontend: "yes"`; value note updated to reference `Overviews → Capability tab`. | CLOSED |

---

## 11. Human Gates

These gaps require a decision, approval, or manual action by a named human authority. Engineering
work is either complete (awaiting human confirmation) or blocked until the decision is made.
No engineering work is underway on these items.

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| HG-01 | Human Gate | HIGH | Individual accountability — choose Option A (individual accounts), Option B (claimed display name), or Option C (defer); record decision with authority name and date | Options in `05_individual_accountability_options.md`; decision record not filled; no individual attribution exists today; Option C (defer) recommended for V1 | HUMAN GATE |
| HG-02 | Human Gate | HIGH | Multi-Wing load test — 250-user test with synthetic second Wing in staging; second Wing must be activated in staging DB; governance gate §0 of Wing onboarding runbook must be passed first | Procedure in `17_multi_wing_load_test_procedure.md`; staging second Wing not provisioned | HUMAN GATE |
| HG-03 | Human Gate | HIGH | CSRF env var verification — System Admin confirms `COOKIE_SAMESITE=none`, `COOKIE_SECURE=true`, and no-wildcard `CORS_ALLOWED_ORIGINS` in Railway production dashboard | **2026-08-16:** Production CORS preflight verified clean — specific origin, no wildcard, credentials allowed. Staging cookie attrs confirmed `SameSite=none; Secure` via live login check. **One step remains:** SA checks Railway production dashboard to confirm `COOKIE_SAMESITE=none` + `COOKIE_SECURE=true` env vars are set (code defaults to `lax`/`false` — env vars are required). | HUMAN GATE |
| HG-04 | Human Gate | HIGH | Backup key custody — GPG private key stored offline, GitHub Secrets updated, daily backup confirmed, first restore test pass reviewed | Steps in `v1_go_no_go_checklist.md §C`; not yet completed by System Admin | HUMAN GATE |
| HG-05 | Human Gate | HIGH | DR rehearsal — System Admin runs disaster recovery rehearsal per `19_disaster_recovery_rehearsal.md`; result recorded in Evidence Table | Procedure complete; never run | HUMAN GATE |
| HG-06 | Human Gate | CRITICAL | External penetration test — vendor selected and engaged; grey-box 5-day engagement completed; critical and high findings remediated before National rollout | Scope in `22_pen_test_scope.md`; vendor not engaged; budget/organisational approval required | HUMAN GATE |
| HG-07 | Human Gate | CRITICAL | Data governance decisions — all 9 decisions resolved: personal data policy, cadet data policy, audit log access, retention periods, archive/delete requirements, incident reporting, production data ownership, support ownership, account removal | Template with 9 pending decisions exists; all 9 unresolved; required before storing personal data at National scale | HUMAN GATE |

---

## 12. Final Engineering Program — Unregistered / Partial Gaps

**Source:** 107-section program brief audit at HEAD `7acb2a8` (2026-08-15). Twelve requirements newly
registered; none previously had an explicit register entry with all required fields. All items were
identified as either NOT IMPLEMENTED or PARTIAL in the audit.

**Implementation priority:** P1 release-safety (MAINT-02, AUTO-01, DATA-CONF-01, FAC-DUP-01) →
P2 operational (CLASS-MATRIX-01, CLASS-FORECAST-01, BULK-01, FAC-SUG-01, PN-WIZ-01) →
P3 usability (DND-01, DENS-01, HOL-01).

| ID | AREA | SEVERITY | REQUIREMENT | CURRENT STATE | STATUS |
|---|---|---|---|---|---|
| MAINT-02 | Maintenance | HIGH | Full maintenance state machine — NORMAL → MAINTENANCE_PENDING → MAINTENANCE_LOCKED → NORMAL — with System Admin workflow: enter title/message/drain duration, confirm, server enters PENDING (new high-impact writes blocked, safe in-flight writes complete), users see "Maintenance will begin shortly — TMS is saving any current changes", after drain enters LOCKED (non-SA logins blocked, active sessions move to maintenance screen), System Admin retains full access throughout. Disable returns to NORMAL. Affected roles: all users receive state; system_admin controls it. Affected interfaces: connected-frontend System Console + maintenance banner + login flow; Planning Workspace banner + API write gate. | Current: binary on/off via `SystemSetting` key `maintenance_mode` with value `"maintenance_enabled"` / absent. DEF-12 (CLOSED): in-session polling banner. DEF-13 (CLOSED): `maintenance_until` stored/displayed. DEF-14 (FIXED LOCALLY): job drain on enable. Missing: no PENDING phase, no drain timer, non-admin logins not blocked in LOCKED state, no title/message stored for user-visible comms. Root cause: DEF-12/13/14 addressed comms and job drain but not the 3-state machine itself. Implementation: `_compute_phase(active, pending_until_iso)` in `main.py`; `maintenance_pending_until` SystemSetting key set on enable (now + drain_seconds, capped 300 s); cleared on disable; `_maintenance_active()` returns 6-tuple with phase; `maintenance_gate` middleware passes PENDING through; `GET /api/system/maintenance` returns phase+pending_until; `GET /api/health/ui-config` returns maintenance_phase+maintenance_pending_until+maintenance_title; connected-frontend `_pollMaintenanceStatus()` polls every 5 s in PENDING (→ 90 s in normal/locked), shows amber ⚠️ banner in PENDING vs red 🔒 in LOCKED; System Console panel shows PENDING/LOCKED label and write-block active status; enable form adds title and drain_seconds fields. Tests: 8 new tests in test_system_admin.py: PENDING phase returned on enable, PENDING allows writes, drain=0 gives LOCKED immediately, disable clears pending_until, ui-config returns phase, SA bypasses LOCKED, LOCKED blocks sqn write, drain cap at 300. Pre-existing 26 tests in test_maintenance_enforcement.py updated to use drain_seconds=0 (skip PENDING) — all pass (1 pre-existing skip). Total: 1719 passed, 6 skipped. | FIXED LOCALLY |
| AUTO-01 | Autosave | MEDIUM | Consistent save-state model across appropriate editable workflows. Forms classified: A (safe autosave with debounce) — local notes, low-risk text metadata, non-destructive preferences; B (explicit Save required) — session fields, facilitator fields, account changes, room/equipment; C (high-impact confirmation required) — archive/delete, role/scope changes, class merge/split, bulk operations, Training Year structural changes. Save-state indicator in plain English: Saving… / Saved / Could not save — Try again. Sensible debounce (≥1400 ms). No write on every keystroke. Prevent duplicate concurrent writes. Optimistic concurrency where version fields exist. Navigation warning when DIRTY on C-form. Regression tests: slow network, save failure, rapid edits, navigation while dirty, duplicate submission, stale version. Affected roles: sqn_admin, wing_admin, national_admin (writers). Affected interfaces: connected-frontend form handlers; Planning Workspace form components. | Current: no save-state indicator anywhere; all forms are fire-and-delete on submit. DEF-23 (2026-08-15, FIXED LOCALLY) added success toasts to 6 previously silent handlers but did not add tracking state. DEF-26 (FIXED LOCALLY) added error handling to Accounts.tsx mutations. Root cause: no save-state abstraction exists in either frontend; each handler independently manages its own optimism. Implementation: `_mkAutoSave(saveFn, statusElId)` factory in connected-frontend (debounce 1400 ms, in-flight lock, READY/DIRTY/SAVING/SAVED/FAILED state, status span `.save-ind`); wired to `pnd-notes` field in Parade Night Detail modal via `addEventListener('input', _mkAutoSave(...))` inside `showPNDetail`'s `setTimeout`; status span rendered as `<span class="save-ind" id="pnd-notes-status">` next to label. Planning Workspace: `SaveIndicator` React component in `components/ui.tsx`; `useAutoSave(saveFn, debounceMs)` hook in `utils/useAutoSave.ts`; wired to session notes textarea in `PlanningRightDrawer` edit-mode path (fires `planningApi.updateSession` with all current form state). Backend: `PATCH /api/parade-nights/{id}` (existing) already supports partial update — notes-only PATCH leaves other fields unchanged (confirmed by `test_partial_notes_patch_round_trips` + `test_partial_notes_patch_clears_notes_with_none`). | FIXED LOCALLY |
| DATA-CONF-01 | Data Quality | MEDIUM | Coherent data-confidence / freshness layer across the application. Support at minimum: DATA CURRENT AS AT, DATA COVERAGE, KNOWN DATA ISSUES — where relevant. Distinguish current / stale / incomplete / unknown / not-applicable. Squadron pages show local freshness (not a meaningless Wing coverage ratio). Wing/National pages show coverage ratio (e.g. "Reporting: 14 of 16 Squadrons"). Do NOT show green simply because data is absent. Potential staleness indicators: unrecorded session outcomes, stale CEA import, incomplete Facilitator data, missing Session outcomes, unresolved data integrity issues, missing required fields, stale activity import. Affected roles: all read roles. Affected interfaces: connected-frontend dashboard, reports, mission backlog, curriculum; Planning Workspace overview/reports cards. | FIXED LOCALLY (2026-08-15). Backend: `_data_freshness(db, scope, sq_id, wing_id)` helper added to `backend/app/routers/dashboard.py`; imported into `planning.py`. Squadron scope: counts unrecorded outcomes (past sessions with non-terminal status), last CEA import age via `MAX(Activity.updated_at WHERE cea_seq_nr IS NOT NULL)`, incomplete facilitator count (active with null/empty subject_areas). Wing/national scope: coverage_pct (% of active squadrons with delivery in last 60 days). Both `/api/dashboard/charts` and `/api/planning/command-centre` return `data_freshness:{as_at, coverage_pct, issues}`. Frontend (`connected-frontend/index.html`): `_renderDataConfBar(freshness, containerId)` function; `data-conf-bar` HTML element on dashboard page; amber warning bar when issues exist, green OK flash when all clear. CSS `.data-conf-bar` + `.dcb-label` + `.dcb-issue` added. 4 regression tests added to `test_planning.py` — all pass. Total: 1725 passed, 6 skipped. | FIXED LOCALLY |
| FAC-DUP-01 | Facilitator | MEDIUM | Replace inline duplicate-Facilitator warning with a structured disambiguation modal. When a same-name match is found on Facilitator create: show "POSSIBLE MATCH — We found another Facilitator with the same name. Existing record: Rank, Given name, Family name, Squadron/scope. Actions: [USE EXISTING FACILITATOR] [CREATE A DIFFERENT PERSON] [MERGE RECORDS] [CANCEL]". Do NOT automatically merge. Do NOT treat Rank as identity (two people may share a name; one person may change rank). MERGE requires permission, shows impact preview (sessions reassigned, leaves transferred, UUID surviving), preserves historical Session references and audit, is reversible only if technically safe. Concurrent same-name creates must be handled (second 409 wins). Affected roles: sqn_admin, wing_admin (facilitator writers). Affected interfaces: connected-frontend `saveFac()` + facilitator modal; Planning Workspace `Facilitators.tsx`. | Current: backend returns 409 `possible_duplicate` with existing record data; `saveFac()` catches 409 and renders an inline warning card with text links "Use existing facilitator" / "Create a different person — these are two people"; no structured modal; no MERGE action in UI; no impact preview; Planning Workspace has similar inline handling. Root cause: original design used inline warning as fastest path; no full disambiguation workflow was built. Implementation: `#m-fac-dup` modal in connected-frontend (4 actions: Cancel / Use Existing / Create Different / Merge Records); `_openFacDupModal(detail, pending)` populates existing-record card + merge preview panel; USE EXISTING calls `showFacProfile(eid)` and closes both modals; CREATE DIFFERENT calls `saveFac(true)` with `confirm_duplicate:true`; MERGE shows impact preview then PATCHes existing record with pending form data via `/api/facilitators/{id}`; MERGE reuses existing `/api/facilitators/{fid}/absorb` POST for full session/leave/audit reassignment; 409 catch in `saveFac()` replaced with structured modal open. | FIXED LOCALLY |
| CLASS-MATRIX-01 | Core Domain | MEDIUM | Curriculum item × Training Class progress matrix — answers: "What has each Training Class completed? What is planned? What needs action?" Rows: curriculum requirements/items. Columns: Training Classes. Cell: text status (Delivered / Planned / Partial / Not started / Cancelled) + colour as secondary cue only. Requirements: accessible table semantics, horizontal scroll container (not body scroll), sticky requirement column, sticky class headings, filter by Stage, filter by Class, filter by status, "action required only" toggle, curriculum item search, click cell for detail (requirement, Training Class, status, elements delivered/outstanding, contributing sessions, next scheduled session, action link). For large configurations: Stage grouping, horizontal windowing/filtering. Affected roles: sqn_admin, wing_admin, national_admin (read); sqn_admin (click-to-plan). Affected interface: connected-frontend curriculum page (new Matrix tab or view). | FIXED LOCALLY (2026-08-15). Backend: `GET /api/curriculum/class-matrix?year_id={id}` added to `training.py`; single joined query loads all `SessionAudience + Session` rows for all classes; builds `{year_id, year, classes:[{class_id,display_name,stage_id,stage_name}], stages:[{stage_id,stage_name,class_ids,items:[{curriculum_id,code,title,cells:{class_id:{status,session_ids,elements_delivered,elements_total}}}]}]}`. Status derived from `_ITEM_STATUS_PRIORITY`. Tenancy: `require_can_view_squadron`. Frontend (`connected-frontend/index.html`): "Matrix ↗" tab added to curriculum tab bar (squadron scope only); `curr-matrix-view` container + `cm-table-wrap`; `openCurrMatrix(btn)` loads year → fetches endpoint → populates stage filter; `_renderCurrMatrix()` applies stage/search/action-required filters and renders sticky-column `<table role="grid">`; 7 cell-status CSS classes (`.cm-cell-delivered` etc.); overflow-x:auto scrolling. 4 regression tests in `test_planning.py` (4 pass, 1 skip — 403 cross-sqn skips due to no second admin fixture). Total: 1730 passed, 7 skipped. | FIXED LOCALLY |
| CLASS-FORECAST-01 | Core Domain | MEDIUM | Per-Training-Class planning forecast — deterministic (no ML). For each Training Class compute: applicable requirements remaining, already planned (scheduled sessions cover the requirement), not yet planned, remaining standard Parade Nights in the Training Year, estimated available instructional Time Blocks, known constraints (facilitator availability, activities removing training opportunities where practical). Status label: ON TRACK (unplanned ≤ available capacity) or PLANNING RISK (more outstanding requirements than available standard training opportunities). Document the exact deterministic rule. Do not imply certainty beyond available data. Affected roles: sqn_admin, Training Officer. Affected interfaces: connected-frontend Mission Backlog / planning overview; Planning Workspace overview. | **Backend:** `GET /api/planning/class-forecasts?year_id={id}` endpoint in `planning.py` — validates year access, loads all TrainingClasses for year, computes `avg_sessions_per_night` from historical PastParadeNight data (defaults to 2), calls `_class_curriculum_progress()` per class to get remaining/planned/unplanned requirements, derives `available_time_blocks = ceil(remaining_parade_nights × avg/total_classes)`, emits status `on_track/planning_risk/critical` per class. Uses `sa_select()` not `.subquery()` to avoid SAWarning. **Frontend:** `_loadClassForecasts(yearId)` in `connected-frontend/index.html` — called alongside `loadMissions()` on year load; fetches endpoint, renders responsive `.fc-grid` of `.fc-card` divs with colored top border (green/amber/red), class name, stage, status pill, stats (unplanned/planned/nights remaining/available blocks), and message; hidden for wing/national scope or 403; `py-forecasts-card` container added below Mission Backlog card; hidden when yearId null in null-branch of `loadYearMap`. **CSS:** `.fc-card`, `.fc-pill`, `.fc-stats`, `.fc-msg` using design-token colors for on-track/planning-risk/critical. **Tests:** 4 regression tests in `test_planning.py` — requires year_id (422), 404 unknown year, empty list for year without classes, structural shape with one class. | FIXED LOCALLY |
| BULK-01 | Workflow | MEDIUM | Apply session structure/template to selected Parade Nights — extends WORK-18 multi-select. Training Officer selects N parade nights → applies a saved template (WORK-17) or copies from an existing parade night. Preview shows: X Time Blocks, Y Sessions across N Parade Nights, Z conflicts detected. Resolution options for conflicts: Skip populated nights / Add alongside existing sessions / Replace selected draft structure. Confirm → apply. Do NOT copy Delivered outcomes or historical completion. Audit each bulk action with batch_id. Affected roles: sqn_admin, Training Officer. Affected interface: connected-frontend parade night list `pn-bulk-bar`. | **Backend:** `POST /api/parade-nights/bulk-apply-template` endpoint in `training.py` — accepts `{template_id, parade_night_ids, conflict_resolution, dry_run}`; `conflict_resolution` ∈ `skip_night/alongside/replace_draft`; dry_run=true runs full logic and returns counts without committing; replace_draft soft-archives sessions with `status∈{planned,rescheduled}` before applying; all applied sessions get `status="planned"` (never copies delivered outcomes); each processed night gets an audit entry with `batch_id`. **Frontend:** "Apply structure…" button added to `pn-bulk-bar`; `openBulkApplyModal()` fetches template list; `_bulkApplyPreview()` calls dry_run=true and shows night/session counts; `_bulkApplyConfirm()` commits and reloads. Modal `m-bulk-apply-tpl` with template picker, conflict resolution select, preview div, confirm button showing night count. **Tests (test_pn_templates.py):** dry_run returns counts without writing; skip_night leaves occupied nights untouched; alongside adds alongside existing; replace_draft removes planned sessions; delivered outcomes not copied (applied status=planned); 404 for unknown template; 401 unauthenticated. 7 new tests, 25 total in file. | FIXED LOCALLY |
| FAC-SUG-01 | Facilitator | MEDIUM | Explainable Facilitator suggestion engine on session assignment. When assigning a Facilitator, rank suitable options by: Instructor Suitability (subject area match), availability (not on leave for that parade date), no current session conflict, current term workload (sessions already allocated), relevant qualifications where held, scope, active status. Show ranked list with per-suggestion explanation: "SUGGESTED: SGT(AAFC) Smith — Suitable for Fieldcraft / Available during this period / No current conflict / 2 sessions allocated this term." Each suggestion must explain WHY. Do not invent qualifications. Do not infer sensitive attributes. Do not silently assign anyone — user must choose. Where data insufficient: "No recommendation available." Do not present random order as intelligent recommendation. Affected roles: sqn_admin, Training Officer. Affected interfaces: connected-frontend session edit modal facilitator select; Planning Workspace session form facilitator field. | **Backend:** `GET /api/sessions/{sid}/facilitator-suggestions?parade_date={date}&period={n}` in `training.py`; loads active facilitators for squadron; scores each by: +5 subject_area match with curriculum item phase, -1000 on leave (PlanningFacilitatorLeave), -500 conflict (another session in same pn+period), -workload (session count this term); sorts descending by score; returns `{suggestions:[{facilitator_id,display_name,rank_label,subject_areas,reasons,conflicts,workload_sessions_this_term}],no_recommendation}`. **Frontend:** `_loadFacSuggestions(sessId, date, period)` added; called non-blocking after `_populateQuickEditClasses` when `quickEdit()` opens; fetches endpoint and renders `.fac-sugg-panel` with SUGGESTED/AVAILABLE/CONFLICT pills and reason strings above the `#qe-fa` select; UNAVAILABLE facilitators hidden from panel; failure is silent (must not break session edit). CSS: `.fac-sugg-panel`, `.fac-sugg-item`, `.fac-sugg-pill.*` variants. **Tests (test_fac_suggestions.py):** shape/fields, unauthenticated 401, unknown 404, conflict flagged, workload counted — 6 tests pass. | FIXED LOCALLY |
| PN-WIZ-01 | Workflow | LOW | Guided six-step Parade Night Builder — accessible to an 80-year-old user with Year 10 English and no technology background. Does NOT remove the existing efficient session form. Adds a guided mode with step-by-step structure: 1 WHO (Training Class/Classes) → 2 WHAT (curriculum item from smart-sorted list) → 3 WHEN (Time Block) → 4 WHO WILL DELIVER (Facilitator, with FAC-SUG-01 suggestions if available) → 5 WHERE (Training Area) → 6 WHAT IS NEEDED (Equipment/resources). Final step: CHECK — facilitator conflict, Training Area conflict, Equipment conflict, curriculum duplication, timing issue, missing requirement. [Save Session]. Experienced users retain Quick entry mode (the existing form). Affected roles: sqn_admin, Training Officer. Affected interfaces: connected-frontend parade night detail modal session creation; Planning Workspace session creation path. | **Frontend (connected-frontend/index.html) — FIXED LOCALLY (2026-08-15 session 19):** `#m-sess-wizard` modal added with 7-dot progress indicator (1–6 + Check), step panels for WHO/WHAT/WHEN/WHO WILL DELIVER/WHERE/WHAT IS NEEDED/CHECK, Back/Next/Save footer. Module-level `_wizState` object holds state across steps. Functions: `_wizOpen(pnId,date)`, `_wizStep(n)`, `_wizNext()`, `_wizBack()`, `_wizCancel()`, `_wizSave()`, `_wizSetupStep1–5`, `_wizSetupCheck()`. Step 1 (WHO): Training Class checkbox list from `S.trainingClasses`; skip checkbox for no-class sessions. Step 2 (WHAT): searchable curriculum list (`_wizRenderCurrList`/`_wizFilterCurr`); custom title mode. Step 3 (WHEN): period number input. Step 4 (WHO WILL DELIVER): FAC-SUG-01 suggestions loaded from existing session on same PN (falls back silently if no session exists); `wiz-fac-sel` populated from `S.facs`. Step 5 (WHERE): `wiz-room-sel` from `S.rooms`. Step 6 (WHAT IS NEEDED): textarea for equipment notes. Check step: summarises all collected state in human-readable rows. Save calls `POST /api/sessions` + `PUT .../audience` with `training_class_ids`. Quick Entry: "Add session" bar at bottom of PN detail modal (when `canWriteSquadron() && !closed`) with `_qeSubmit()` inline function — period picker, curriculum/fac/room selects. CSS: `.wiz-dot`, `.wiz-q`, `.wiz-hint`, `.wiz-class-item`, `.wiz-curr-item`, `.wiz-fac-pill-row`, `.wiz-summary-row`, `.add-sess-bar`, `.qe-form`. No backend changes needed (existing `POST /api/sessions` + `PUT .../audience` cover all wizard fields). **Tests (test_pn_wizard.py):** minimal session creates (period only); full fields (curriculum, facilitator, room); custom title with null curriculum; audience assignment via `training_class_ids`; multiple periods same night; unauthenticated 401; missing PN returns error. 7 tests — all pass. Backend: 1753 passed, 7 skipped. | FIXED LOCALLY |
| DND-01 | Planning Workspace | LOW | Drag-and-drop scheduling in Planning Workspace — drag Session → another Time Block on same Parade Night, Session → another Parade Night, Session → another Training Class (where valid), unscheduled Mission Backlog item → planning slot. Do NOT make DnD the only scheduling mechanism — click/form scheduling (WORK-08) and explicit Edit Session must remain. Pre-drop validation: scope, Time Block availability, Training Class match, Facilitator conflict, Training Area conflict, Equipment conflict, archived/inactive state; invalid drop shows visual rejection. Post-drop: persist via `PUT /api/sessions/{id}`, show Saving… → Saved; rollback visual position on save failure; Undo where practical. Keyboard move workflow as accessibility alternative (existing WORK-08 "Move to another night" form satisfies this). Use HTML5 DnD API or established project dependencies if justified; prefer existing React project dependencies; audit WCAG implications before adopting a library. Affected roles: sqn_admin, Training Officer. Affected interfaces: Planning Workspace `EightWeekView`, `TermView`, `ParadeNightBlock` session cells. | **FIXED LOCALLY (2026-08-15 session 19):** HTML5 drag-and-drop added to `ParadeNightBlock.tsx` and `EightWeekView.tsx`. `DragSessionPayload` interface exported from `ParadeNightBlock`: carries `{session_id, cadet_group, curriculum_id, facilitator_id, location_id, activity_title, status}` — all fields needed to re-issue `trainingApi.editSession` with the same session data at the new position. Filled session cells get `draggable={true}` and `onDragStart` when `onMoveSession` prop is provided AND `cell.source` (PlanningSession) is available; payload serialized via `e.dataTransfer.setData('application/json', ...)`. Empty cells get `onDragEnter`/`onDragLeave` (with enter-count ref to suppress child-element flicker), `onDragOver`, and `onDrop`; drop target shows dashed-blue outline and "Drop here" label. `EightWeekView` receives `handleMoveSession(payload, targetDateId, targetPeriod)` built with `useCallback`; calls `trainingApi.editSession` (same endpoint as WORK-08 move-form); on success: `toast("Session moved.")` + invalidates `planning-long-range` and `planning-night-summaries` query caches; on failure: `toast(friendlyMessage(e, ...))` — session stays in original position. Keyboard alternative: WORK-08 "Move to another night" form in `PlanningRightDrawer` (already FIXED LOCALLY). TermView not wired (uses `fromNightSummary` path without `source` data; WORK-08 form covers that path). TypeScript: 0 errors. | FIXED LOCALLY |
| DENS-01 | Visual | LOW | User-selectable display density preference. Options: Comfortable (default, recommended for most users) / Compact (shows more information on screen). User-facing label: "Display size" — not "density". Example: "Display size — ● Comfortable ○ Compact." Persist per user/device via `sessionStorage` (presentation preference only, not operational data). Both modes must be accessible, responsive, and usable at 200% zoom. Do not store sensitive information in browser storage. Affected roles: all. Affected interfaces: connected-frontend settings section; Planning Workspace preferences panel. | **FIXED LOCALLY (2026-08-15 session 20):** Both frontends implement density toggle. **connected-frontend:** `body[data-density="compact"]` CSS block in `connected-frontend/index.html` overrides form-group margin, button padding, card padding, modal padding, nav-item padding/font-size. "Display Size" radio card added to `page-settings` after Session Structure card: two radio buttons Comfortable (default) and Compact. JS: `_setDensity(val)` sets/removes `document.body.dataset.density`, persists to `sessionStorage.displayDensity`, syncs radio UI; IIFE reads `sessionStorage` on page load; `renderSettings()` syncs radio on settings page open. **Planning Workspace:** `AppShell.tsx` — `density` state initialised from `sessionStorage.displayDensity`; `applyDensity(d)` updates state + writes `sessionStorage`; "Size: comfortable/compact" toggle button added to topbar right (next to Theme button); root `.app` div receives `data-density="compact"` when compact (React JSX prop — attribute removed when `undefined`, no DOM mutation needed). CSS in `frontend/src/styles/layout.css`: `.app[data-density="compact"]` overrides `.main` padding (24px→14px), `.nav-item` padding/font-size, `.nav-group` margin. CSS in `frontend/src/styles/components.css`: `.app[data-density="compact"]` overrides `.card`, `.ctitle`, `.btn`, `.modal-body`, `.modal-head`, `.form`/`.inline-form` gap+label margin, `.grid` gap, `.stat` padding. TypeScript: 0 errors. sessionStorage key: `displayDensity` (shared between both frontends — toggling in one carries to the other on session cross-link). | FIXED LOCALLY |
| HOL-01 | Holiday | LOW | `statutory_holiday` user-facing label — `_HOL_TYPE_LABELS` dict in `connected-frontend/index.html` maps holiday type codes to user-facing strings; the key `statutory_holiday` is absent despite being used in `second_wing_seed.py`; any record with this type displays raw snake_case `"statutory_holiday"` to users. Determine whether `statutory_holiday` and `public_holiday` are the same concept (canonicalise safely) or genuinely distinct (document distinction, add label). Do NOT add a label without checking for duplicate concepts. Add regression test. Affected roles: all (read). Affected interfaces: connected-frontend holiday list, calendar display, weekly program print. | FIXED 2026-08-15: Source audit confirmed `statutory_holiday` and `public_holiday` are distinct string values (not aliases) — `second_wing_seed.py` uses `statutory_holiday` for Remembrance Day etc.; `seed_all.py` uses `public_holiday` for equivalent records; both exist in production data. They are semantically related (both are days of observance) but not identical (statutory holiday has legal standing under the National Defence Act). Decision: add label rather than canonicalise seed data (which could affect existing records). `'statutory_holiday': 'Statutory Holiday'` added to `_HOL_TYPE_LABELS` dict at `connected-frontend/index.html:12164`. Regression test `test_statutory_holiday_type_round_trips` added to `backend/tests/test_planner_v14.py` — verifies API accepts and returns the type verbatim. Test passes (1 passed). Commit: pending HOL-01 commit with FAC-DUP-01 group. | FIXED LOCALLY |
| MAINT-03 | Maintenance | MEDIUM | System Admin login during maintenance LOCKED — when `block_logins=True` and phase=LOCKED, system_admin must always be able to log back in (e.g. after session expiry) so they can disable maintenance. The middleware's existing SA bypass requires a valid JWT in the request, but a login request carries no JWT — so the bypass never fires for logins, leaving SA locked out of their own system. Fix: `/api/auth/login` moved to `_MAINTENANCE_ALWAYS_EXEMPT` (middleware never inspects it); handler calls `_check_maintenance_login_gate(role, db)` after access code verified and role known — SA always passes, non-SA in LOCKED phase gets 503 `maintenance_mode`. Circular import avoided by inlining the 8-line phase logic in `auth.py` (importing from `main.py` would be circular since `main.py` imports routers). Affected roles: system_admin (must pass), all others (blocked in LOCKED). Affected files: `backend/app/main.py`, `backend/app/routers/auth.py`, `backend/tests/test_system_admin.py`, `backend/tests/test_maintenance_enforcement.py`. | FIXED LOCALLY 2026-08-16. Implementation: `/api/auth/login` added to `_MAINTENANCE_ALWAYS_EXEMPT` frozenset in `main.py`; `_check_maintenance_login_gate(role, db)` added to `auth.py`; called in `login()` after `record_login_success_db` and user active check; SA returns immediately; non-SA checks `maintenance_mode` and `maintenance_block_logins` SystemSettings then inlines phase computation; raises 503 in LOCKED only. Tests: `test_maintenance_block_logins_blocks_sqn_admin` (enable maintenance drain=0, block_logins=True → ADMIN703 login 503 with maintenance_mode); `test_maintenance_block_logins_allows_sysadmin_login` (same config → SYSTEMADMIN2026 login 200); `test_maintenance_block_logins_pending_allows_all` (drain=300 → ADMIN703 login 200 — PENDING does not block). 1 pre-existing test updated in `test_maintenance_enforcement.py` (`test_block_logins_true_blocks_login_during_maintenance`): assertion format changed from bare `r.json()["error"]` to flexible `body.get("error") or body.get("detail",{}).get("error")` to handle HTTPException-wrapped format. Total: 1756 passed, 7 skipped. | FIXED LOCALLY |

---

## Level Gate Summary

### Level A — 7 Wing Operational V1

**Status: DEPLOYED** — commit `756e65e`, production, 2026-08-12. Alembic head v51 confirmed.
All engineering gates closed. Human post-release actions due within 7 days:

| Action | Item | Due |
|---|---|---|
| H2 | Fill named ownership table in `25_support_runbook.md` Part 1 | Within 7 days |
| H3 | Confirm first weekly restore test GitHub Action passed | Within 7 days |
| H4 | Schedule first quarterly DR rehearsal | Within 7 days |
| H5 | Communicate V1 go-live to 7WG beta testers | Within 7 days |

Human gates HG-01 (individual accountability decision), HG-03 (CSRF env vars), HG-04 (backup key custody), HG-07 (data governance) remain open per `v1_go_no_go_checklist.md`.

### Level B — Second Wing Pilot (additional requirements beyond Level A)

> ~~CLASS-12, CLASS-13, CLASS-14~~ — Training Class frontend gaps: all three CLOSED (2026-08-13).  
> ~~DEF-10, DEF-11~~ — DB-backed rate limiter: FIXED LOCALLY (2026-08-13); no longer a Level B gate.

| Gaps | Description | Status |
|---|---|---|
| DEF-06, DOC-12 | Wing onboarding API (not CLI-only) | FIXED LOCALLY |
| DEF-07, DOC-11 | Multi-Wing reports cross-scope verified | FIXED LOCALLY |
| HG-01 | Individual accountability model implemented | HUMAN GATE |
| HG-02 | 250-user multi-Wing load test | HUMAN GATE |
| HG-06 | External pen test (recommended before second Wing) | HUMAN GATE |
| HG-07 | Data governance decisions resolved | HUMAN GATE |
| VIS-01 | Shared design tokens complete | FIXED LOCALLY |
| VIS-10 | Multi-Wing E2E playwright tests | FIXED LOCALLY |

### Level C — National Readiness (additional requirements beyond Level B)

> VIS-02, VIS-09, DEF-08, DEF-09, SEC-05, SEC-06, DASH-03 — all FIXED LOCALLY (codebase changes applied; require staging verification post-Level-B-deploy).

| Gaps | Description | Status |
|---|---|---|
| SEC-04 / HG-06 | External pen test REQUIRED (not only recommended) | HUMAN GATE |
| DOC-11 | Tier 3 National reports implemented and verified | FIXED LOCALLY |
| VIS-02, VIS-09 | Axe automation in main CI; WCAG 2.1 AA formal declaration | FIXED LOCALLY |
| DEF-08, DEF-09 | Async export pipeline complete with frontend polling | FIXED LOCALLY |
| SEC-05, SEC-06 | Full alerting stack operational | FIXED LOCALLY |
| DASH-03 | Full metric dictionary documentation | FIXED LOCALLY |

---

## Decision Log

| Gap | Decision | Date | Authority |
|---|---|---|---|
| GAP-03 | Adopt `training_areas` as canonical location; `planning_locations` deprecated as adapter; Phase 2 table drop deferred to Level B | 2026-08-12 | `04_canonical_data_model.md` |
| GAP-05 | Individual accountability: Option C (defer to Level B/National) recommended for V1 | — | MANUAL APPROVAL REQUIRED — CO / Wing SOCAD |
| GAP-20 | CSRF: CORS + SameSite=None sufficient; CSRF tokens not required given Bearer token architecture; production env var verify still required by System Admin | 2026-08-12 | `20_csrf_assessment.md` |
| GAP-22 | External pen test budget and scope | — | MANUAL APPROVAL REQUIRED |
| GAP-23 | All 9 data governance decisions | — | MANUAL GOVERNANCE REQUIRED — CO / Data Authority |
| WRITE-* | Training Summary → Dashboard merge authorised; Training Summary nav tab may be removed only after content/action parity is proven and a redirect is preserved; no other feature removal pre-authorised | 2026-08-04 | User instruction; recorded in `.claude/rules/capability-preservation.md` |

---

## Cross-Reference: Original 25-Gap Matrix to This Register

| Original Gap | Title | Register IDs | Status |
|---|---|---|---|
| 1 | Legacy page retirement | DEF-05 | CLOSED |
| 2 | Visual / session unification | VIS-01, VIS-10 | FIXED LOCALLY |
| 3 | TrainingArea / PlanningLocation | Decision log | CLOSED |
| 4 | Facilitator records | (Capability preserved; no open gap) | CLOSED |
| 5 | Individual accountability | HG-01 | HUMAN GATE |
| 6 | Optimistic locking | (Phase 7 complete; all critical models) | CLOSED |
| 7 | 7WG hardcodes | (Bootstrap parameterised) | CLOSED |
| 8 | Multi-Wing onboarding | DEF-06, DOC-12 | FIXED LOCALLY |
| 9 | Multi-Wing reports | DEF-07 | FIXED LOCALLY |
| 10 | Report catalogue | DASH-01 (CLOSED), DASH-02 (CLOSED), DOC-11 (FIXED LOCALLY — Level B) | FIXED LOCALLY |
| 11 | Year rollover | DOC-02 | STAGING VERIFIED |
| 12 | Playwright E2E | DOC-03, VIS-10 | FIXED LOCALLY |
| 13 | Accessibility automation | VIS-02–VIS-09 | FIXED LOCALLY |
| 14 | Load testing | HG-02 | HUMAN GATE |
| 15 | Distributed rate limiting | DEF-10, DEF-11 | FIXED LOCALLY |
| 16 | Async imports/exports | DEF-08 (FIXED LOCALLY), DEF-09 (FIXED LOCALLY) | FIXED LOCALLY |
| 17 | Monitoring and alerting | SEC-05, SEC-06, SEC-07, SEC-13 | FIXED LOCALLY |
| 18 | Maintenance mode | DEF-12, DEF-13, DEF-14 | FIXED LOCALLY |
| 19 | Session revocation | SEC-11 | FIXED LOCALLY |
| 20 | CSRF controls | SEC-01, HG-03 | HUMAN GATE |
| 21 | Backup and restore | SEC-02, SEC-03, HG-04, HG-05 | HUMAN GATE |
| 22 | External pen test | SEC-04, HG-06 | HUMAN GATE |
| 23 | Data governance | HG-07 | HUMAN GATE |
| 24 | Beta feedback register | DOC-08 | HUMAN GATE |
| 25 | Support runbook | DOC-01, DOC-04 | CLOSED |
