# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
*.egg-info/
dist/
build/
.eggs/
pip-wheel-metadata/

# SQLite databases and WAL files
*.db
*.db-shm
*.db-wal

# Backups
backups/

# Test / coverage artefacts
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Logs
*.log
logs/

# Distribution packages / delivery ZIPs
*.zip
*.tar.gz
*.whl

# Node
node_modules/
frontend/dist/
frontend/dist-single/
*.tsbuildinfo

# Local-dev-only patched copy served by RUN_TMS_CONNECTED_FRONTEND_MAC.sh
# (never the checked-in connected-frontend/index.html itself)
connected-frontend/.local-dev/

# macOS
.DS_Store
.AppleDouble
.LSOverride
# Finder duplicate copies (e.g. "filename 2.ext") created when a file is
# dragged into a folder that already contains one with the same name.
*\ 2.*
*\ 2/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Claude Code IDE — include project rules, exclude local/session files
.claude/settings.local.json
.claude/todos.json
.claude/*.jsonl
.claude/projects/
.claude/worktrees/

# Stress / smoke test scripts contain hardcoded dev seed codes — never commit
tools/stress/
frontend/test-results/
backend/.venv
frontend/node_modules
tools/playwright-staging/node_modules
